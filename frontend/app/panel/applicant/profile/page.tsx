/**
 * Mój profil. Empty on purpose: what the profile holds depends on decision
 * R-01 (docs/runbook/rozbieznosci.md), so there is nothing to guess here yet.
 */
export default function ProfilePage() {
  return (
    <section className="flex flex-col gap-3">
      <h1 className="text-2xl">Mój profil</h1>
      <p className="text-sm">Ten ekran powstaje. Wkrótce znajdziesz tu dane swojego podmiotu.</p>
    </section>
  );
}
