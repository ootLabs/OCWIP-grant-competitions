# Mapa: frontend

Aplikacja Next.js (App Router, TypeScript, Tailwind CSS). Wzorce: [`../konwencje.md`](../konwencje.md).

| Plik | Co robi |
|---|---|
| `frontend/package.json` | Skrypty `dev`, `build`, `start`, `typecheck`, `api:generate` (klient z OpenAPI, adres nadpisywalny przez `OPENAPI_URL`), `test`. Next 15, React 19, Tailwind 4, Vitest, openapi-typescript |
| `frontend/tsconfig.json` | Tryb strict, alias `@/*` na katalog główny frontu |
| `frontend/next.config.mjs` | `reactStrictMode` plus watch przez polling, bo źródło jest bind mountem i zdarzenia inotify giną |
| `frontend/postcss.config.mjs` | Podpięcie `@tailwindcss/postcss` (Tailwind 4 nie potrzebuje pliku konfiguracyjnego) |
| `frontend/vitest.config.mts` | Vitest z jsdom i pluginem React, alias `@` zgodny z tsconfig |
| `frontend/app/globals.css` | Import Tailwinda plus tokeny w bloku `@theme` (kolory, fonty, odstępy, promienie). Tryb kontrastu przez `[data-contrast="true"]`, nadpisujący te same tokeny. **Jedyne miejsce na kolory**, komponenty ich nie wpisują. Reguły bazowe (`body`, nagłówki, `a`) siedzą w `@layer base`, więc klasa narzędziowa na pojedynczym elemencie je nadpisuje; obramowanie fokusu celowo zostaje poza warstwami |
| `frontend/app/layout.tsx` | Rama aplikacji, metadane, `lang="pl"`, wczytanie fontów `Playfair Display`/`Poppins` przez `next/font/google` (podzbiory `latin` i `latin-ext` pod polskie znaki) |
| `frontend/app/page.tsx` | Strona startowa szkieletu z linkami do sond zdrowia API, do publicznej listy konkursów i do `/design-tokens` |
| `frontend/app/not-found.tsx` | Strona 404 dla całej aplikacji (T-15.4): własny wygląd i powrót na stronę główną, bo trafia tu też osoba niezalogowana |
| `frontend/app/error.tsx` | Granica błędu Next.js, czyli 500 widziane przez użytkownika (T-15.4). **Nie renderuje ani `message`, ani `digest`.** Dwa wyjścia: `reset()` na miejscu i powrót na stronę główną |
| `frontend/app/global-error.tsx` | Ten sam 500 dla awarii samego layoutu głównego: własne `html`, `body` i import `globals.css`, bo zastępuje layout. Wyjściem jest `reset()`, router padł razem z layoutem. Fonty marki nie przeżywają tej awarii (`next/font` woła layout), więc jawne `font-sans` |
| `frontend/app/error-pages.test.tsx` | Testy stron błędów: droga powrotna z 404, brak wycieku treści błędu na 500, ponowienie na miejscu, ten sam ekran przy awarii layoutu |
| `frontend/app/design-tokens/page.tsx` | Podgląd tokenów brandingowych OCWIP (T-15.1): logo, kolory z liczonym na żywo kontrastem WCAG, typografia, odstępy, promienie, przyciski |
| `frontend/app/design-tokens/contrast-toggle.tsx` | Klientowy przełącznik podglądu trybu wysokiego kontrastu, ustawia `data-contrast="true"` na otaczającym `div` |
| `frontend/app/panel/applicant/layout.tsx` | Trasa `/panel/applicant` (cel przekierowania z `/login`): serwerowa obwoluta trzymająca tytuł strony, bo rama panelu jest klientowa i nie może wystawić `metadata` |
| `frontend/app/panel/panel-gate.tsx` | **Strażnik sesji wspólny dla obu paneli** (T-15.2, wydzielony w T-15.3): pyta `GET /me`, brak sesji przekierowuje na `/login` z `returnUrl` niosącym query string, cudza rola dostaje odmowę zamiast przekierowania, padnięty backend osobny komunikat. Na czas pytania rysuje szkielet ramy podany przez panel w `skeleton` (T-15.4), zamiast wyśrodkowanego komunikatu, który przesuwał layout. Wpuszczoną sesję oddaje ramie przez `children(session)`, a ekranowi odmowy dokłada link do własnego panelu odmówionego konta, jeśli taki istnieje. Nie jest kontrolą dostępu, tą jest domyślna odmowa z T-13.2 |
| `frontend/app/panel/panel-skeleton.tsx` | Kształt panelu rysowany, dopóki `GET /me` nie odpowie (T-15.4): te same odstępy co prawdziwy nagłówek, szerokość wiersza z `rowClassName` i po jednym miejscu na każdy realny link z `links`, pasek trybu rezerwowany pustym blokiem przez `modeBar`, zero nazw kont, pulsowanie tylko przy `motion-safe` |
| `frontend/app/panel/panel-skeleton.test.tsx` | Testy szkieletu: komunikat dla czytnika ekranu, dekoracja ukryta przez `aria-hidden`, pasek trybu tylko dla operatora i bez napisu, liczba miejsc w nawigacji równa liczbie linków, brak animacji przy ograniczonym ruchu |
| `frontend/app/panel/navigation.ts` | Kształt pozycji nawigacji (`PanelLink`), `isCurrentLink` z jawnym korzeniem panelu oraz `panelRootForRole`, czyli frontowe lustro `Services/LoginLandingPath.cs`, celowo niepełne (rola bez zbudowanego panelu nie dostaje linku). Listy linków zostają przy panelach |
| `frontend/app/panel/navigation.test.ts` | Testy dopasowania bieżącej trasy, w tym że korzeń jest parametrem, a nie wpisaną na sztywno ścieżką wnioskodawcy |
| `frontend/app/panel/empty-screens.test.tsx` | Test wspólny dla sześciu ekranów obu paneli, które nadal są zwykłym stanem pustym: każdy ma tytuł, stan pusty z podpowiedzią następnego kroku i nic technicznego w treści. "Formularze" wypadł w T-26, bo dostał własne stany (`operator/forms/page.test.tsx`) |
| `frontend/app/panel/applicant/applicant-panel.tsx` | Rama panelu wnioskodawcy (T-15.2): wpuszcza `PanelGate` z rolą `Applicant`, link "przejdź do treści", nagłówek, `<main id="tresc">` o szerokości formularza |
| `frontend/app/panel/applicant/panel-header.tsx` | Nagłówek panelu: logo, nazwa zalogowanego podmiotu, wylogowanie, nawigacja z `aria-current` na bieżącej pozycji. Dziś żadna ścieżka w produkcie nie przypina podmiotu do konta (R-01), więc w praktyce widać imię i nazwisko z konta |
| `frontend/app/panel/applicant/navigation.ts` | Pozycje nawigacji panelu wnioskodawcy jako dane. **Jedyne miejsce ze ścieżkami tego panelu** |
| `frontend/app/panel/applicant/page.tsx` | Moje wnioski, stan pusty z następnym krokiem prowadzącym do konkursów (lista wniosków to T-34) |
| `frontend/app/panel/applicant/competitions/page.tsx` | Aktualne konkursy, stan pusty na czas między naborami. Listy tu nie ma celowo: jest publiczna, pod `/competitions` (T-23, decyzja D6), a ten ekran na nią wyprowadza |
| `frontend/app/panel/applicant/profile/page.tsx` | Mój profil, stan pusty mówiący, skąd wezmą się dane podmiotu; zawartość czeka na decyzję R-01 |
| `frontend/app/panel/applicant/applicant-panel.test.tsx` | Testy ramy: nazwa podmiotu i wylogowanie w nagłówku, komplet nawigacji, link pomijający przed nagłówkiem, brak sesji na logowanie z `returnUrl`, brak mignięcia panelu, wylogowanie po stronie serwera, odmowa dla operatora, awaria backendu nie jest wylogowaniem |
| `frontend/app/panel/applicant/navigation.test.ts` | Test, że każda pozycja nawigacji zostaje wewnątrz panelu wnioskodawcy |
| `frontend/app/panel/operator/layout.tsx` | Trasa `/panel/operator` (cel przekierowania z `/login` dla roli Operator): serwerowa obwoluta trzymająca tytuł strony, bo rama panelu jest klientowa |
| `frontend/app/panel/operator/operator-panel.tsx` | Rama panelu operatora (T-15.3): wpuszcza `PanelGate` z rolą `Operator`, cudza rola widzi 403 z wyjaśnieniem. `<main id="tresc">` bez ograniczenia szerokości, z `overflow-x-auto`, żeby tabela szersza od okna przewijała się wewnątrz zamiast poszerzać dokument i wywozić przyklejony nagłówek w lewo |
| `frontend/app/panel/operator/operator-header.tsx` | Nagłówek panelu operatora: przyklejony, zaczyna się paskiem trybu na tokenach `active-bg`/`active-text` (przeżywa tryb wysokiego kontrastu), potem logo, "Zalogowano jako", wylogowanie i nawigacja z `aria-current` |
| `frontend/app/panel/operator/navigation.ts` | Pozycje nawigacji panelu operatora jako dane. **Jedyne miejsce ze ścieżkami tego panelu.** Osiem zakładek wewnątrz konkursu to poziom niżej i należy do T-22 |
| `frontend/app/panel/operator/page.tsx` | Konkursy (T-22): lista wszystkich konkursów operatora (szkice i nieaktywne włącznie, `ListCompetitions` nic nie chowa), stan pusty pierwszego dnia pracy z systemem, wejście "Nowy konkurs" do kreatora. Wypadła z `app/panel/empty-screens.test.tsx` z tego samego powodu co `operator/forms/page.tsx` w T-26: czyta realne dane, nie stoi pusta |
| `frontend/app/panel/operator/page.test.tsx` | Testy listy: stan pusty, wejście do kreatora zawsze widoczne, status konkursu po polsku, link do formularza, ponowienie po błędzie sieci |
| `frontend/app/panel/operator/competitions/new/page.tsx` | Kreator ogłoszenia konkursu (T-22): siedem kroków plus podsumowanie, orkiestruje zapis (szkic w `localStorage` do pierwszego zapisu, potem `createCompetition`/`updateCompetition`), publikację (`publishCompetition`) i odczyt katalogu pracowników. Krok 0 (kopia z poprzedniego roku) i edycja istniejącego konkursu świadomie nie tu, patrz komentarz na górze pliku i `docs/log.md` |
| `frontend/app/panel/operator/competitions/new/page.test.tsx` | Testy całego przepływu: zdanie o terminie zamknięcia na żywo, kwota słownie, brak zapisu bez pól strukturalnych, zapis szkicu, podgląd dokładnie taki jak strona publiczna, publikacja wymaga potwierdzenia (nie jednego kliknięcia), szkic czyszczony po publikacji, błąd z backendu trafia do właściwego kroku |
| `frontend/app/panel/operator/competitions/new/wizard-nav.tsx` | Siedem kroków plus podsumowanie jako lista klikalna w dowolnej kolejności (walidacja nie blokuje nawigacji), z kropką przy kroku, którego dotyczy błąd z backendu |
| `frontend/app/panel/operator/competitions/new/save-bar.tsx` | Pasek stanu zapisu: kiedy ostatnio zapisano jako roboczy, przycisk "Zapisz", brakujące pola strukturalne blokujące zapis, ogólny błąd, lista kroków do sprawdzenia |
| `frontend/app/panel/operator/competitions/new/field-error.tsx` | Jeden błąd walidacji backendu pod polem, którego dotyczy |
| `frontend/app/panel/operator/competitions/new/suffixed-number-field.tsx` | Wspólna otoczka pola liczbowego z sufiksem (T-22, krok 1.4), pod `AmountField` i `PercentField`: etykieta jako osobny element wskazujący `id`, nie owijający `<label>`, inaczej sufiks i podpowiedź wchodzą do nazwy dostępności pola i zmieniają się przy każdym wpisanym znaku |
| `frontend/app/panel/operator/competitions/new/amount-field.tsx` | Pole kwotowe z kwotą słownie pod spodem (T-22, krok 1.4). `amountInWords` liczy słownie tylko poniżej miliarda, więc kwota z tej i większej wartości dostaje brak podpowiedzi zamiast wysadzić kreator wyjątkiem (`RangeError` złapany przed mergem własnym review) |
| `frontend/app/panel/operator/competitions/new/amount-field.test.tsx` | Testy pola: zwykła kwota, kwota o wartości miliarda lub większej nie wysadza renderu, podpowiedź aktualizuje się przy wpisywaniu |
| `frontend/app/panel/operator/competitions/new/percent-field.tsx` | Pole procentowe (krok 1.4), cienka otoczka na `SuffixedNumberField` |
| `frontend/app/panel/operator/competitions/new/step-basics.tsx` | Krok 1.1 Dane konkursu: numer, tytuł, terminy, nabór ciągły, informacja po złożeniu, zdanie o tym, co znaczy wpisany termin zamknięcia (`describeDeadline`), wejście do kreatora formularza wniosku, dostępne dopiero po zapisaniu konkursu |
| `frontend/app/panel/operator/competitions/new/step-description.tsx` | Krok 1.2 Opis konkursu: opis, zakładane rezultaty, adres regulaminu |
| `frontend/app/panel/operator/competitions/new/step-paper.tsx` | Krok 1.3 Forma dostarczenia: przełącznik papieru z terminem i adresem, widocznymi tylko po włączeniu |
| `frontend/app/panel/operator/competitions/new/step-limits.tsx` | Krok 1.4 Limity, najważniejszy krok kreatora: ramy projektu, cztery kwoty (`AmountField`), dwa procenty (`PercentField`), podstawa liczenia procentu, data przetwarzania danych osobowych, kategorie kosztów |
| `frontend/app/panel/operator/competitions/new/step-attachments.tsx` | Krok 1.5 Załączniki do oferty: limity uploadu w MB, lista załączników (`AttachmentRow`) dodawana i usuwana, błędy indeksowane z backendu (`attachments[N].pole`) rozbite na poszczególne wiersze |
| `frontend/app/panel/operator/competitions/new/attachment-row.tsx` | Jeden wymagany załącznik: tytuł, opis, wymagalność, dopuszczalne formaty. `groupName` przekazywane z zewnątrz, żeby dwa wiersze o tym samym (pustym) tytule nie dzieliły grupy radiową |
| `frontend/app/panel/operator/competitions/new/step-contacts.tsx` | Krok 1.6 Osoby kontaktowe: wybór z katalogu pracowników (`GET /accounts/operators`), treść e-maila po złożeniu wniosku |
| `frontend/app/panel/operator/competitions/new/step-summary.tsx` | Krok 1.7 Podsumowanie i publikacja: podgląd na `saved` (dane faktycznie zapisane przez backend) tymi samymi komponentami co publiczna strona konkursu (`CompetitionFacts`, `CompetitionAttachments`, `CompetitionContacts`), "Popraw" przy każdej sekcji, publikacja z jawnym dwuetapowym potwierdzeniem |
| `frontend/app/panel/operator/applications/page.tsx` | Wnioski (T-35): wybór konkursu, bo wnioski wpływają do konkretnego naboru; konkursy w wersji roboczej pominięte, stan pusty z przejściem do konkursów, ładowanie i ponowienie |
| `frontend/app/panel/operator/applications/page.test.tsx` | Testy wyboru konkursu: link do listy, pominięty szkic, stan pusty, ponowienie pytające sieć jeszcze raz |
| `frontend/app/panel/operator/applications/[competitionId]/page.tsx` | Lista wniosków jednego konkursu (T-35): ładowanie, błąd z ponowieniem, 404 jako "nie ma takiego konkursu", stan pusty bez eksportów, linki do eksportu CSV i PDF prosto z API |
| `frontend/app/panel/operator/applications/[competitionId]/application-table.tsx` | Tabela wniosków: kolumny z raportu, sortowanie po każdej kolumnie poza Lp. (`aria-sort`), filtry statusu i rodzaju wnioskodawcy, suma wnioskowanych kwot (dla widocznych wierszy pod filtrem) i reszta puli w stopce. `table-fixed` z szerokościami kolumn, żeby nic nie skakało |
| `frontend/app/panel/operator/applications/[competitionId]/page.test.tsx` | Testy listy: 120 wierszy i kolumny z raportu, sumy, link do oferty, sortowanie w obie strony z liczbą porządkową według widoku, filtr z sumą widocznych, brakujące wartości, eksporty, stan pusty, 404 |
| `frontend/app/panel/operator/applications/[competitionId]/[applicationId]/page.tsx` | Złożona oferta (T-35): kto i kiedy złożył, blok techniczny (wersja formularza, suma kontrolna), załączniki do pobrania, odpowiedzi przez `OfferView`; szkic i cudzy wniosek jako 404 |
| `frontend/app/panel/operator/applications/[competitionId]/[applicationId]/page.test.tsx` | Testy oferty: nagłówek, rodzaj, suma kontrolna, link do załącznika, odpowiedzi i tabela budżetu z wyliczoną wartością, pominięte pole ukryte warunkiem, 404 |
| `frontend/app/panel/operator/forms/page.tsx` | Lista konkursów operatora ze statusem formularza (ma / brak) i wejściem do kreatora (T-26) |
| `frontend/app/panel/operator/forms/page.test.tsx` | Testy listy: stan pusty, rozróżnienie konkursu z formularzem i bez, komunikat błędu z możliwością ponowienia |
| `frontend/app/panel/operator/forms/[competitionId]/page.tsx` | Kreator formularza (T-26): wczytuje szkic z `localStorage`, w jego braku własną aktualną wersję konkursu, w jej braku prowadzi przez wybór konkursu źródłowego do skopiowania. Każda zmiana zapisuje szkic i dokłada poprzedni dokument do stosu cofania. Przełącznik Edycja/Podgląd i `PublishPanel` (T-27); po udanej publikacji czyści szkic, bo opublikowana wersja przestaje być czymkolwiek roboczym |
| `frontend/app/panel/operator/forms/[competitionId]/page.test.tsx` | Testy kreatora na prawdziwym przepływie: kopiowanie z konkursu źródłowego, edycja etykiety, dodanie pola, blokada usunięcia pola używanego przez warunek widoczności, zmiana kolejności, przeżycie szkicu po ponownym zamontowaniu strony |
| `frontend/app/panel/operator/forms/[competitionId]/builder.tsx` | Pasek stanu szkicu (zapisano o, cofnij, odrzuć) plus lista sekcji |
| `frontend/app/panel/operator/forms/[competitionId]/source-picker.tsx` | Wybór konkursu, z którego kopiuje się formularz; stan pusty, gdy żaden konkurs go jeszcze nie ma |
| `frontend/app/panel/operator/forms/[competitionId]/preview-panel.tsx` | Podgląd (T-27): woła `FormRenderer` z T-28 na dokumencie kreatora, bez podpięcia zapisu. Przycisk "od nowa" remontuje renderer pustymi odpowiedziami |
| `frontend/app/panel/operator/forms/[competitionId]/publish-panel.tsx` | Publikacja (T-27, `POST` z T-25): potwierdzenie inline zamiast natywnego okna, błędy bramki kontraktu ze ścieżką JSON, `detail` z 409 pokazany wprost, bo backend pisze go świadomie dla operatora. Stan wraca do przycisku, gdy dokument się zmieni po publikacji albo po odrzuceniu |
| `frontend/app/panel/operator/forms/[competitionId]/section-editor.tsx` | Edycja tytułu, opisu i warunku widoczności sekcji, lista jej pól, dodawanie pola. Bez dodawania, usuwania i przestawiania sekcji (zawężenie T-26, `T-26a` w kolejce) |
| `frontend/app/panel/operator/forms/[competitionId]/field-row.tsx` | Jedno pole zwinięte do podsumowania: etykieta, rodzaj, przesunięcie góra/dół, usunięcie zablokowane, gdy inne pole się do niego odwołuje (warunek, obliczenie, limit) |
| `frontend/app/panel/operator/forms/[competitionId]/field-editor.tsx` | Wspólne ustawienia pola (etykieta, podpowiedź, wymagalność, flaga wydruku D14) plus rozgałęzienie na ustawienia właściwe rodzajowi |
| `frontend/app/panel/operator/forms/[competitionId]/role-picker.tsx` | Wybór roli pola (T-35), "Pokazuj na liście wniosków jako": tylko role pasujące do rodzaju pola, rola zajęta przez inne pole wyłączona z jego etykietą. Nie ma go na kolumnie tabeli |
| `frontend/app/panel/operator/forms/[competitionId]/options-editor.tsx` | Lista opcji wyboru jednej/wielu: etykieta wpisywana, wartość na drucie generowana z niej |
| `frontend/app/panel/operator/forms/[competitionId]/table-editor.tsx` | Kolumny tabeli (zwykłe pola z zawężonym zestawem rodzajów), widełki wierszy dla tabeli o zmiennej liczbie wierszy |
| `frontend/app/panel/operator/forms/[competitionId]/calculation-editor.tsx` | Sposób liczenia i składniki pola wyliczanego wskazywane z listy, nigdy jako wzór tekstem. Suma przyjmuje co najmniej jeden składnik (T-31) |
| `frontend/app/panel/operator/forms/[competitionId]/limits-editor.tsx` | Reguły limitu (D12): rodzaj i to, względem czego jest mierzony, w tym ustawienie konkursu z przedrostkiem `competition.`. Procent limitu to stała liczba albo próg z ustawień konkursu (`percentFrom`, T-31) |
| `frontend/app/panel/operator/forms/[competitionId]/limits-editor.test.tsx` | Wybór źródła procentu: próg konkursu zamiast wpisanej liczby, ukrycie pola liczby przy progu z konkursu, zmiana na limit kwotowy czyści oba pola procentu |
| `frontend/app/panel/operator/forms/[competitionId]/visible-when-editor.tsx` | Warunek widoczności sekcji i pola jako wybór pola plus wartości, współdzielony przez oba miejsca |
| `frontend/app/panel/operator/forms/[competitionId]/add-field-control.tsx` | "Dodaj pole": rodzaj z listy, etykieta wpisana, klucz generowany bez udziału operatora |
| `frontend/app/panel/operator/reviewers/page.tsx` | Recenzenci, stan pusty (obsługa recenzentów to T-37) |
| `frontend/app/panel/operator/operator-panel.test.tsx` | Testy ramy operatora: komplet nawigacji, pasek trybu i jego miejsce w kolejności czytania, 403 dla wnioskodawcy i dla recenzenta, brak mignięcia ramy, `returnUrl` z query string, wylogowanie po stronie serwera, awaria backendu nie jest wylogowaniem, link wyjścia dla wnioskodawcy i jego brak dla recenzenta, tabela 120 wierszy razem z ograniczeniem przewijania |
| `frontend/app/panel/operator/navigation.test.ts` | Test, że każda pozycja nawigacji zostaje wewnątrz panelu operatora |
| `frontend/app/competitions/layout.tsx` | Rama publicznych stron konkursu (T-23): woła `PublicFrame` z `components/public-frame.tsx`, tę samą co ekrany konta |
| `frontend/app/competitions/page.tsx` | Publiczna lista ogłoszonych konkursów, renderowana na serwerze, więc działa bez JavaScriptu i w podglądzie odnośnika. Co widzi gość, decyduje backend: szkic nie ma adresu, archiwalny wypada z listy, ale adres zachowuje |
| `frontend/app/competitions/[id]/page.tsx` | Strona pojedynczego konkursu plus `generateMetadata` ze znacznikami Open Graph (tytuł, kwota, termin). Brak konkursu i szkic to jednakowe 404. Treść operatora renderowana jako tekst, nigdy jako znaczniki. Bez `og:url`, bo publiczny adres jest faktem wdrożenia, nie builda |
| `frontend/app/competitions/competition-card.tsx` | Jeden konkurs na liście: numer, stan, tytuł jako link, zdanie reguły naboru i maksymalna dotacja. Linkiem jest sam tytuł, nie cała karta |
| `frontend/app/competitions/competition-facts.tsx` | Terminy, kwoty, procenty, ramy projektu, forma papierowa i limity uploadu jako lista definicji. Parametr, którego nikt nie ustawił, znika zamiast pokazywać kreskę |
| `frontend/app/competitions/competition-attachments.tsx` | `CompetitionAttachments` i `CompetitionContacts`: co przygotować i kogo zapytać. Wzorów plików tu nie ma, bo schemat nie ma jeszcze miejsca na plik (T-32) |
| `frontend/app/competitions/intake-countdown.tsx` | Klientowy stan naboru: zdanie z reguły T-21 renderowane zawsze, licznik dopiero po zamontowaniu i tylko gdy jest co liczyć. Odświeżanie co 30 sekund, bez sekundnika. Przy zniknięciu terminu licznik jest czyszczony, nie zostawiany |
| `frontend/app/competitions/apply-link.tsx` | Przycisk "Wypełnij wniosek" prowadzący na `/login` z `returnUrl` wracającym na ten sam konkurs. Widoczny wyłącznie przy `intake.acceptsApplications`; zamknięty nabór dostaje zdanie, nie wyszarzony przycisk |
| `frontend/app/competitions/competition-permalink.tsx` | Klientowe pole ze stałym adresem konkursu i przyciskiem kopiowania, z jawnym potwierdzeniem w `aria-live`. Adres bezwzględny powstaje w przeglądarce, bo serwer go nie zna |
| `frontend/app/competitions/labels.ts` | Polskie etykiety enumów konkursu jako pełne mapy `Record`, więc wartość dopisana do API przestaje się kompilować zamiast wpaść w gałąź domyślną |
| `frontend/app/(account)/layout.tsx` | Rama ekranów konta (T-12.7, T-12.8), grupa tras `(account)`: jedna `PublicFrame` dla `/login`, `/register`, `/verify-email`, `/forgot-password` i `/reset-password`, bez "(account)" w adresach |
| `frontend/app/(account)/login/page.tsx` | Trasa `/login` (T-12.7): serwerowo czyta `returnUrl` przez `firstParam` i podaje go formularzowi, bez `useSearchParams` i bez Suspense |
| `frontend/app/(account)/login/login-form.tsx` | Formularz logowania (T-12.7): e-mail i hasło z autouzupełnianiem, komunikat z `loginFailureMessage` w `role="alert"`, hasło czyszczone po odmowie serwera, zostawiane po awarii sieci. Po sukcesie `router.replace` na `redirectPath` z backendu, nigdy na cel złożony we froncie. Przy 403 (adres niepotwierdzony) link do ponownej wysyłki. Linki do resetu hasła i rejestracji (z `returnUrl`) |
| `frontend/app/(account)/login/login-form.test.tsx` | Testy formularza: etykiety i autouzupełnianie, przekierowanie wyłącznie tam, gdzie wskazał backend, jeden komunikat przy 401 z wyczyszczonym hasłem, 403, 429 z czasem blokady, awaria bez treści technicznych i z zachowanym hasłem, `returnUrl` przy linku do rejestracji, link do ponownej wysyłki tylko przy 403 |
| `frontend/app/(account)/register/page.tsx` | Trasa `/register` (T-12.8): serwerowo czyta `returnUrl` i podaje go formularzowi |
| `frontend/app/(account)/register/register-form.tsx` | Formularz rejestracji (T-12.8): imię, nazwisko, e-mail, hasło (`new-password`) z podpowiedzią polityki. Po 202 jeden ekran "sprawdź skrzynkę", ten sam dla adresu wolnego i zajętego i bez powtórzenia adresu. Z tego ekranu link do ponownej wysyłki, gdy mail nie dotarł. Błędy pól z backendu przy polach, hasło czyszczone tylko wtedy, gdy odrzucono hasło. `returnUrl` idzie do backendu i dalej do maila |
| `frontend/app/(account)/register/register-form.test.tsx` | Testy: autouzupełnianie, `returnUrl` w żądaniu, ekran po wysłaniu bez adresu, komunikaty polityki hasła przy polu (`aria-invalid`, `aria-describedby`), 429, awaria z zachowanym hasłem, `returnUrl` przy linku do logowania |
| `frontend/app/(account)/verify-email/page.tsx` | Trasa `/verify-email` (T-12.8), cel linku z maila: czyta `userId`, `token` i `returnUrl` |
| `frontend/app/(account)/verify-email/verify-email.tsx` | Potwierdzenie adresu przy wejściu na stronę, **jedno żądanie na stronę** (strażnik w `useRef`, bo drugi POST tym samym tokenem daje 400 i zamieniłby sukces w błąd). Po sukcesie link do `/login` z `returnUrl`. Po odmowie zdanie z backendu, link do logowania (adres mógł być już potwierdzony) i ponowna wysyłka; po awarii sieci "Spróbuj ponownie". Otwarta bez `userId` i `token` to prośba o nowy link, bez komunikatu o błędzie; bez jednego z nich to link niepełny. Żaden z tych stanów nie woła backendu |
| `frontend/app/(account)/verify-email/resend-verification-form.tsx` | Ponowna wysyłka linku z `returnUrl`: jedna odpowiedź dla każdego adresu, bez jego powtórzenia |
| `frontend/app/(account)/verify-email/verify-email.test.tsx` | Testy: jedno żądanie pod `StrictMode`, link do logowania z `returnUrl`, martwy link ze zdaniem backendu, link bez tokenu bez żądania, ponowienie po awarii, ponowna wysyłka z `returnUrl` i tą samą odpowiedzią |
| `frontend/app/(account)/forgot-password/page.tsx` | Trasa `/forgot-password` (T-12.8) |
| `frontend/app/(account)/forgot-password/forgot-password-form.tsx` | Prośba o link do resetu: zawsze ta sama odpowiedź, bez powtórzenia adresu, 429 ze zdaniem backendu, awaria bez treści technicznych |
| `frontend/app/(account)/forgot-password/forgot-password-form.test.tsx` | Testy: ta sama odpowiedź dla nieznanego adresu, 429, 503 bez treści technicznych |
| `frontend/app/(account)/reset-password/page.tsx` | Trasa `/reset-password` (T-12.8), cel linku z maila resetu: czyta `userId` i `token` |
| `frontend/app/(account)/reset-password/reset-password-form.tsx` | Nowe hasło z tokenu: 400 z `fieldErrors.newPassword` zostawia formularz z błędem przy polu, 400 bez nich (link martwy) zastępuje formularz drogą do nowego linku. Po sukcesie informacja o wylogowaniu wcześniejszych sesji i link do logowania |
| `frontend/app/(account)/reset-password/reset-password-form.test.tsx` | Testy: sukces z ciałem żądania, polityka hasła przy polu, martwy link bez formularza, link bez tokenu, awaria z zachowanym hasłem |
| `frontend/app/competitions/competitions.test.tsx` | Testy obu stron publicznych: lista bez konta, stan pusty, nabór ciągły, komplet treści na stronie konkursu, podstawa liczenia procentu, pominięty parametr, powrót z logowania na ten sam konkurs, brak przycisku po zamknięciu, 404 dla adresu bez konkursu, struktura nagłówków, znaczniki Open Graph |
| `frontend/app/competitions/intake-countdown.test.tsx` | Testy licznika: odliczanie, brak sekund, zatrzymanie w minucie zamknięcia, nabór ciągły, wygaszony timer po odmontowaniu |
| `frontend/app/competitions/competition-permalink.test.tsx` | Testy odnośnika: adres bezwzględny w polu, potwierdzenie kopiowania, przeglądarka odmawiająca dostępu do schowka |
| `frontend/components/empty-state.tsx` | `EmptyState`: tytuł, zdanie o tym, czego brakuje, i opcjonalny następny krok. Używany przez wszystkie puste ekrany obu paneli (T-15.4) |
| `frontend/components/offer-view.tsx` | `OfferView`: złożona oferta tylko do odczytu (T-35), sekcje i pola widoczne według warunków, tabele z wierszami i wartościami wyliczanymi. Osobny komponent, nie renderer z wyłączonymi polami, bo wyłączone pole czytnik ogłasza jako niedostępne i ucina długi tekst |
| `frontend/components/status-page.tsx` | `StatusPage`: komunikat na cały ekran zamiast treści (odmowa, awaria backendu, 404, 500), `aria-live`, droga powrotna w `actions` i wspólny styl tej drogi w `statusActionClassName`. Wydzielony z `panel-notice.tsx` w T-15.4 |
| `frontend/components/account-field.tsx` | `AccountField`: pole ekranu konta (T-12.8) z etykietą, podpowiedzią i komunikatami backendu podpiętymi przez `aria-describedby` i `aria-invalid`. Plus `accountSubmitClassName`, jeden przycisk dla wszystkich ekranów konta |
| `frontend/components/public-frame.tsx` | `PublicFrame`: rama stron bez sesji, czyli publicznych stron konkursu (T-23) i ekranów konta (T-12.7, T-12.8). Link pomijający z `focus:fixed`, nagłówek z logo, `<main id="tresc">` o szerokości czytelnej na telefonie, stopka. Celowo nie rama panelu: tu nie ma nazwy konta ani wylogowania. Wydzielona z `app/competitions/layout.tsx` w T-12.7 |
| `frontend/components/form-renderer/form-renderer.tsx` | `FormRenderer`: druga strona kontraktu z `T-24` (T-28). Zamienia `FormDocument` plus odpowiedzi w działający, paginowany formularz: nawigacja między sekcjami, stos `touched` do bramkowania błędów, operacje na wierszach tabeli idące przez `resolveTableRows` i `reorder` z `lib/forms/`. Używany bez zmian przez podgląd `T-27` i przez ścieżkę wnioskodawcy `T-34`, żaden z nich nie buduje własnej kopii |
| `frontend/components/form-renderer/form-renderer.test.tsx` | Testy renderera: każdy z piętnastu rodzajów pól, pole warunkowe pokazujące się i chowające bez utraty wcześniej wpisanej wartości, pole wyliczane przeliczające się na żywo, tabela zmienna (dodawanie, kolejność, usuwanie) i stała (bez tych operacji), błąd trzymający się właściwego wiersza po przestawieniu, limit D12 z konkretną kwotą |
| `frontend/components/form-renderer/renderer-context.tsx` | `RendererProvider`/`useRenderer`: dokument, odpowiedzi, `touched` i wszystkie operacje w jednym miejscu, żeby nie przekazywać ich polem po polu przez sekcję, pole i komórkę. `cellKey`/`parseCellKey` i przestawienie `touched` po usunięciu albo przestawieniu wiersza tabeli (`touchedAfterRowRemoved`, `touchedAfterRowsSwapped`) |
| `frontend/components/form-renderer/section-view.tsx` | Pola jednej sekcji, jedna kolumna, czytane od góry do dołu (`proces.md`, zasada 5). Pole tabeli pomija wspólny wrapper etykiety, bo ma własny układ |
| `frontend/components/form-renderer/section-nav.tsx` | Pasek sekcji ze stanem każdej: gotowa, w toku, są błędy (`proces.md`, zasada 9). Sekcja ukryta przez własny warunek widoczności znika z paska całkowicie, nie jako wyszarzona pozycja |
| `frontend/components/form-renderer/field-view.tsx` | Etykieta (albo `fieldset`/`legend` dla grupy radiowej), pole, podpowiedź, licznik znaków i błąd jednego pola. Błąd widoczny dopiero po dotknięciu pola, poza polem wyliczanym, którego jedyny błąd (limit) nie ma jak zostać dotknięty samodzielnie |
| `frontend/components/form-renderer/field-input.tsx` | Sam kontrolek, jedna gałąź na rodzaj pola z piętnastu (bez tabel i grupy wyboru, które mają własne pliki). Pole wyliczane dostaje już policzoną wartość z `field-view.tsx`, nie czyta jej z odpowiedzi samo |
| `frontend/components/form-renderer/choice-input.tsx` | Grupa radiowa albo checkboxowa: `yesNo`, `singleChoice`, `multipleChoice`. Konwersja `boolean` na `"true"`/`"false"` idzie przez `yesNoWireValue`/`yesNoFromWireValue` z `document-types.ts`, nie własną kopię |
| `frontend/components/form-renderer/table-field.tsx` | `repeatableTable` i `fixedTable`: nagłówek, wiersze przez `resolveTableRows` (fixedTable nie ma nic w odpowiedziach, dopóki komórka nie zostanie dotknięta), dodawanie/usuwanie/przestawianie tylko dla tabeli zmiennej |
| `frontend/components/form-renderer/table-cell.tsx` | Jedna komórka: kontrolka dopasowana do rodzaju kolumny, kolumna wyliczana wyszarzona i licząca się na żywo przez `computeRowValue`, własny błąd niezależny od pozostałych komórek wiersza |
| `frontend/lib/api-schema.ts` | **Generowany**, nie edytuj: typy `paths`, `components` i `operations` z dokumentu OpenAPI backendu. Odtwarzany przez `npm run api:generate` |
| `frontend/lib/api-client.ts` | `apiBaseUrl`, `serverApiBaseUrl`, `apiFetch`, `ApiError`, typy `ApiPath`, `ApiRequestInit`, `ProblemDetails`, `ValidationProblemDetails`, `FieldErrors` z `api-schema.ts`. Jedyne wejście do API: `credentials: "include"` dla ciasteczka sesyjnego, `ApiError.message` celowo generyczny, błędy pól z `problem+json` na `ApiError.fieldErrors`, puste ciało (202, 204) zwracane jako `undefined`. `baseUrl` w `init` wybiera adres API, bo komponent serwerowy stoi w kontenerze i nie dosięga adresu przeglądarki (T-23). `ApiError.detail` (T-27) niesie `ProblemDetails.detail`, gdy backend napisał go świadomie do pokazania (na przykład dwa różne powody 409 przy publikacji formularza); domyślnie `null`, bo generyczny komunikat zostaje domyślny wszędzie indziej |
| `frontend/lib/api-client.test.ts` | Testy klienta: fallback adresu API, wysyłanie poświadczeń, brak wycieku ciała odpowiedzi do komunikatu błędu, błędy walidacji przypięte do pól, odpowiedź bez ciała |
| `frontend/lib/local-storage.ts` | Jedyne miejsce czytające i piszące `localStorage`: `readJson`, `writeJson`, `removeItem`, współdzielone przez `forms/draft-storage.ts` (T-26) i `competition-wizard/draft-storage.ts` (T-22), wydzielone z tego drugiego przy review, bo obie kopie różniły się tylko kluczem i kształtem koperty. Każda funkcja degraduje się do braku zamiast rzucać: okno prywatne z zablokowanym zapisem i renderowanie po stronie serwera są normalnym stanem, nie awarią |
| `frontend/lib/local-storage.test.ts` | Testy: zapis i odczyt, brak klucza jako `null`, uszkodzony wpis jako `null`, usunięcie, brak `localStorage` w środowisku, rzucający `setItem` połknięty |
| `frontend/lib/competitions.ts` | Odczyt publicznego konkursu w jednym miejscu (T-23): `fetchPublicCompetitions`, `fetchPublicCompetition` (404 z backendu zamieniane na `null`), `competitionPath` i typy z `api-schema.ts`. Zawsze `cache: "no-store"`, bo stan naboru zmienia sam zegar i nie ma czego unieważniać. O tym, czy nabór trwa, nie decyduje tu nic: to jedzie w `intake` z reguły T-21 |
| `frontend/lib/competitions.test.ts` | Testy odczytu: adres API brany z `API_SERVER_URL`, brak cache, identyfikator wstawiony i zescapowany do jednego segmentu ścieżki, 404 jako `null`, 500 nadal wyjątkiem |
| `frontend/lib/operator-competitions.ts` | Konkurs oczami operatora (T-22): `fetchOperatorCompetitions`, `fetchOperatorCompetition`, `createCompetition` (zawsze szkic), `updateCompetition`, `publishCompetition` (jedyne przejście, o jakie prosi kreator: `POST .../status` z `Published`), `fetchOperators` (katalog do wyboru osoby kontaktowej, krok 1.6). Osobno od `competitions.ts`: tamten jest anonimowym odczytem publicznym wołanym z serwera, ten idzie z przeglądarki z ciasteczkiem sesji |
| `frontend/lib/operator-competitions.test.ts` | Testy klienta operatora: właściwa trasa dla każdej funkcji, identyfikator w adresie, ciało żądania przy tworzeniu/edycji, publikacja wysyła wyłącznie `{status: "Published"}` |
| `frontend/lib/operator-applications.ts` | Wnioski oczami operatora (T-35): `fetchApplicationList`, `fetchSubmittedApplication`, adresy eksportów i załączników w API, polskie etykiety rodzaju i statusu, sortowanie i filtrowanie w przeglądarce (`visibleRows`: numer liczbowo, polska kolejność liter, puste na końcu w obu kierunkach) |
| `frontend/lib/operator-applications.test.ts` | Testy sortowania, filtrów, sumy widocznych i adresu eksportu |
| `frontend/lib/competition-wizard/types.ts` | Własny kształt roboczy kreatora konkursu (T-22): `CompetitionDraft` trzyma same stringi, dokładnie takie, jakie oddaje input (data, kwota, tekst), nic nie jest parsowane w trakcie wypełniania. `WIZARD_STEPS`/`WizardStepId`/`STEP_LABELS` to siedem kroków po polsku i po angielsku jako identyfikator |
| `frontend/lib/competition-wizard/draft-factory.ts` | `newDraft()`: pusty szkic z tymi samymi wartościami domyślnymi co `CompetitionRequest` bez pola (kategorie kosztów, limity uploadu 10/50 MB), żeby operator widział limit, który już obowiązuje, zamiast pustego pola |
| `frontend/lib/competition-wizard/draft-storage.ts` | Szkic w `localStorage` przez `lib/local-storage.ts`, jeden slot na "nowy konkurs" (nie jeden na konkurs jak w `forms/draft-storage.ts`, bo przed pierwszym zapisem konkurs nie ma jeszcze identyfikatora). `competitionId` w kopercie startuje jako `null` i wypełnia się po pierwszym udanym zapisie |
| `frontend/lib/competition-wizard/to-request.ts` | `toCompetitionRequest`: zamienia szkic na `CompetitionRequest`. Rozróżnia brak STRUKTURALNY (pól, bez których żądania nie da się zbudować: numer, tytuł, data startu, data końca albo nabór ciągły, maksymalna dotacja - sprawdzane tu, przed jakimkolwiek żądaniem) od niekompletności BIZNESOWEJ (reszta, sprawdzana dopiero przez backend przy zapisie, zgodnie z proces.md). Kwoty i procenty jadą jako tekst, nie liczba, żeby nie zgubić grosza na drucie |
| `frontend/lib/competition-wizard/to-request.test.ts` | Testy konwersji: braki strukturalne, nabór ciągły bez daty końca, konwersja czasu lokalnego na UTC, puste pole opcjonalne jako `null`, forma papierowa włączona i wyłączona, megabajty na bajty, załączniki i osoby kontaktowe |
| `frontend/lib/competition-wizard/field-steps.ts` | `stepForField`/`stepsForFields`: mapa klucza błędu z `CompetitionRequestValidator` (w tym `attachments[N].pole`) na krok kreatora, żeby błąd z backendu wylądował przy właściwym polu, nie w ogólnym komunikacie |
| `frontend/lib/competition-wizard/field-steps.test.ts` | Testy mapowania: pole płaskie, pole załącznika z indeksem, nieznane pole na krok podsumowania, deduplikacja listy kroków |
| `frontend/lib/competition-wizard/intake-sentence.ts` | `describeDeadline` (T-22, krok 1.1: "ekran mówi wprost, co oznacza wpisany termin zamknięcia"). Celowo NIE reguła T-21: ta odpowiada "czy nabór trwa teraz" dla konkursu, który już istnieje, a tu chodzi wyłącznie o to, co znaczy właśnie wpisana data, więc drugiej kopii tamtej reguły nie ma |
| `frontend/lib/competition-wizard/intake-sentence.test.ts` | Testy zdania: brak daty, sformatowana data i godzina słowami, nabór ciągły |
| `frontend/lib/session.ts` | `fetchCurrentUser` (401 to brak sesji, nie awaria), `logout` (błąd przełknięty, żeby dało się opuścić ekran), `accountLabel`, typy `CurrentUser` i `Role`. Jedyne miejsce czytające `GET /me` |
| `frontend/lib/session.test.ts` | Testy sesji: żywa sesja, 401 jako brak sesji, 500 nadal wyjątkiem, wylogowanie po stronie serwera i jego odporność na błąd, etykieta konta |
| `frontend/lib/login.ts` | Logowanie z frontu (T-12.7): `login` (wysyła `returnUrl` do backendu bez filtrowania, bo o bezpieczeństwie celu decyduje `LoginLandingPath.cs`), `loginFailureMessage` (401, 403 i 429 z polskim zdaniem z backendu, 400 jak złe dane, 5xx i sieć jako "spróbuj za chwilę", nigdy jako złe hasło), `isRefusal`, `withReturnUrl`, ścieżki `/register` i `/forgot-password` |
| `frontend/lib/account.ts` | Ekrany konta poza logowaniem (T-12.8): `register`, `verifyEmail`, `resendVerification`, `forgotPassword`, `resetPassword` oraz `accountFailure` (400 z polami przy polach, 400 bez pól i 429 ze zdaniem backendu albo ogólnym "nie przyjął tych danych", 5xx i sieć jako "spróbuj za chwilę", z flagą `refused` do czyszczenia hasła), `passwordHint` |
| `frontend/lib/account.test.ts` | Testy: ciało i trasa każdego wywołania, `returnUrl` bez zmian, mapowanie każdego rodzaju błędu |
| `frontend/lib/search-params.ts` | `firstParam` i typ `SearchParams`: odczyt parametru adresu na serwerze przez ekrany konta, pierwszy z powtórzonych, pusty jako null |
| `frontend/lib/search-params.test.ts` | Testy `firstParam` |
| `frontend/lib/login.test.ts` | Testy: ciało żądania z nietkniętym `returnUrl`, komunikat dla każdego statusu, awaria nigdy jako złe hasło, rozróżnienie odmowy od awarii, kodowanie `returnUrl` w linku |
| `frontend/lib/format.ts` | Data, godzina, kwota i procent tak, jak wychodzą na ekran (T-23). Strefa przypięta na `Europe/Warsaw`, więc serwer i przeglądarka piszą to samo; obraz bez bazy stref schodzi na UTC i mówi to wprost, lustrzanie do `CompetitionIntakeMessage` w backendzie. `formatDateOnly` nie przepuszcza daty kalendarzowej przez `Date`, bo zrobiłaby z niej północ UTC. Strefa rozstrzygana raz, przez `resolvedOptions`, a nie przez złapanie wyjątku: obraz bez danych stref potrafi po cichu oddać UTC |
| `frontend/lib/format.test.ts` | Testy formatowania: czas letni i zimowy po obu stronach października, data kalendarzowa bez godziny, kwota z OpenAPI jako liczba i jako tekst, limit rozmiaru pliku |
| `frontend/lib/countdown.ts` | `remainingUntil` i `formatRemaining`: ile zostało do zamknięcia naboru (T-23). Nie odpowiada, czy nabór trwa, bo to robi reguła z T-21; bierze moment zamknięcia i moment bieżący jako argumenty, nie czyta zegara. Bez sekund, obcina zamiast zaokrąglać, trzy formy liczby mnogiej |
| `frontend/lib/countdown.test.ts` | Testy licznika: obcinanie, minuta zamknięcia jako `null`, niepoprawny moment jako `null`, formy 1/2/5/12/22, brak sekund |
| `frontend/lib/contrast.ts` | `relativeLuminance`, `contrastRatio`, `meetsAA` - kalkulator kontrastu WCAG 2.1 użyty do weryfikacji tokenów narzędziem, nie ręcznie |
| `frontend/lib/contrast.test.ts` | Testy kalkulatora kontrastu na parach kolorów z researchu brandingu (karta T-07) |
| `frontend/lib/local-time.ts` | `localWarsawToUtcIso` (T-22): zamienia wartość z `<input type="datetime-local">`, czyli czas lokalny wpisany przez operatora w kreatorze ogłoszenia, na instant UTC wysyłany do API. Offset liczony `Intl.DateTimeFormat` dla konkretnej chwili, nie ze stałej tabeli +01:00/+02:00, więc obie strony zmiany czasu wychodzą poprawnie |
| `frontend/lib/local-time.test.ts` | Testy konwersji: zima, lato, domyślna północ bez podanej godziny, obie strony zmiany czasu w marcu i w październiku |
| `frontend/lib/amount-in-words.ts` | `amountInWords`, `integerInWords` (T-22, krok 1.4): kwota słownie po polsku, odmiana złotego i grosza przez trzy formy liczby mnogiej (`pluralForm`), zaokrąglenie do grosza przed rozbiciem na złote i grosze. Ogólny moduł, nie tylko dla kreatora: ta sama treść wejdzie do umowy w T-45 |
| `frontend/lib/amount-in-words.test.ts` | Testy: zero, jedynki, nastki, dziesiątki, setki, "tysiąc" bez "jeden", tysiące i miliony z własną liczbą mnogą, liczba kończąca się na jeden powyżej dwudziestu bierze dopełniacz mnogi ("dwadzieścia jeden złotych"), grosze, zaokrąglenie szumu zmiennoprzecinkowego, wejście jako string z OpenAPI |
| `frontend/lib/forms/document-types.ts` | Kontrakt definicji formularza po stronie frontu (T-26, T-28): piętnaście rodzajów pól, sekcja, dokument, warunek, obliczenie, limit, plus stałe konwencji drutu (`yesNoWireValue`/`yesNoFromWireValue`, `competitionBasis`/`competitionBasisSetting`). Ręcznie zsynchronizowany z `backend/src/Ocwip.Api/Models/Forms/FormFieldType.cs` i [`docs/kontrakt-formularza.md`](../kontrakt-formularza.md), bo backend czyta tę kolumnę jako nieprzezroczysty `JsonElement` i nic tu nie generuje typów |
| `frontend/lib/forms/document-keys.ts` | `isValidKey`, `slugifyKey`, `uniqueKey`, `allFieldKeys`: klucz pola generowany z etykiety, nigdy wpisywany |
| `frontend/lib/forms/document-keys.test.ts` | Testy generatora klucza: składanie polskich znaków diakrytycznych, prefiks przy etykiecie zaczynającej się cyfrą, unikalność |
| `frontend/lib/forms/document-factory.ts` | `newSection`, `newField`: domyślny, poprawny kształt każdego z piętnastu rodzajów pól |
| `frontend/lib/forms/document-factory.test.ts` | Testy fabryk: domyślne wartości dla każdego rodzaju pola |
| `frontend/lib/forms/document-edit.ts` | Czyste funkcje edycji dokumentu po `FieldPath` (sekcja, pole, opcjonalnie kolumna tabeli): `updateField`, `addFieldToSection`, `addColumnToTable`, `removeField`, `moveField`, `findField` |
| `frontend/lib/forms/document-edit.test.ts` | Testy edycji: pole i kolumna tabeli osobno, przesunięcie na granicy listy jako operacja bez efektu |
| `frontend/lib/forms/document-references.ts` | `findReferencesTo`: kto w dokumencie czyta dany klucz (warunek, obliczenie, limit), z rozróżnieniem klucza krótkiego wewnątrz tabeli i kwalifikowanego na zewnątrz. Używane przed usunięciem albo przesunięciem pola |
| `frontend/lib/forms/document-references.test.ts` | Testy skanu odwołań, w tym odwołanie do kolumny tabeli z zewnątrz i z sąsiedniej kolumny |
| `frontend/lib/forms/document-candidates.ts` | Co operator może wybrać z listy zamiast wpisać: `visibilityCandidates`, `conditionValueOptions`, `calculationOperandCandidates` (suma czyta kolumny tabel i, od T-31, inne pola liczbowe sekcji), `limitBasisCandidates` |
| `frontend/lib/forms/document-candidates.test.ts` | Testy kandydatów, w tym regresja na sekcję nieoferującą samej siebie jako źródło własnego warunku |
| `frontend/lib/forms/labels.ts` | Polskie etykiety enumów kontraktu formularza, wzorem `app/competitions/labels.ts` |
| `frontend/lib/forms/field-roles.ts` | Które role pasują do pola i które pole już ma daną rolę, te same reguły co `FormFieldRole.cs` |
| `frontend/lib/forms/field-roles.test.ts` | Testy dopasowania ról do rodzaju pola i wskazania pola, które rolę już ma |
| `frontend/lib/forms/answer-text.ts` | Jedna odpowiedź jako tekst do czytania (T-35): kwota, procent, data, tak albo nie, etykiety wyborów, nazwa pliku; null, gdy nie odpowiedziano |
| `frontend/lib/forms/answer-text.test.ts` | Testy: brak odpowiedzi, etykiety zamiast wartości wyboru, "Nie" jako odpowiedź, data bez przesunięcia o dzień |
| `frontend/lib/forms/draft-storage.ts` | Szkic kreatora w `localStorage` przez `lib/local-storage.ts`, jeden na konkurs: `loadDraft`, `saveDraft`, `clearDraft`. Backend nie ma endpointu szkicu (`T-24`/`T-25` celowo go nie mają), więc "przeżywa zamknięcie przeglądarki" (kryterium `T-26`) jest rozwiązane tutaj, nie w bazie |
| `frontend/lib/forms/draft-storage.test.ts` | Testy szkicu: puste odczyty, przetrwanie po zapisie, odporność na uszkodzony wpis |
| `frontend/lib/forms/competition-forms.ts` | Odczyty operatorskie kreatora: `fetchOperatorCompetitions`, `competitionsWithForms`, `fetchCurrentFormDocument`. Bez zapisu, publikacja wersji to `T-27` |
| `frontend/lib/forms/competition-forms.test.ts` | Testy odczytów: filtrowanie konkursów z formularzem, wybór wersji oznaczonej jako aktualna |
| `frontend/lib/forms/answer-types.ts` | Kształt odpowiedzi wnioskodawcy (T-28): `FormAnswers`, `AnswerValue`, `TableRowAnswers`. Decyzja renderera, nie kontrakt, bo `applications.answers` czeka na `T-30`. `resolveTableRows` liczy wiersze tabeli tak samo dla tabeli zmiennej (z odpowiedzi) i stałej (ze struktury `table.rows`, zanim ktokolwiek dotknie komórki) |
| `frontend/lib/forms/evaluate.ts` | Silnik w chwili odpowiadania: `isFieldVisible`/`isSectionVisible` (warunek widoczności), `computeTopLevelValue`/`computeRowValue` (pole wyliczane, z zabezpieczeniem przed cyklem). `allTopLevelFields` eksportowane, żeby nie liczyć płaskiej listy pól drugi raz w `limits.ts` |
| `frontend/lib/forms/evaluate.test.ts` | Testy silnika: warunek na `yesNo`/wyborze/wielokrotnym wyborze, suma kolumny tabeli, pole wyliczane zależne od innego pola wyliczanego (D11) |
| `frontend/lib/forms/limits.ts` | D12 po stronie odpowiedzi: `evaluateLimit` liczy konkretną kwotę graniczną i to, ile zostało, nie tylko prawdę albo fałsz. `CompetitionLimitSettings` to sześć ustawień konkursu, jakie kreator już zna z `document-types.ts`. Procent limitu z `percentFrom` czyta ustawienie konkursu, a puste ustawienie znaczy "limitu nie sprawdzamy" (T-31) |
| `frontend/lib/forms/limits.test.ts` | Testy limitu: odwracalność (D12), `maxPercentOf` względem ustawienia konkursu i względem innego pola, brak ustawienia jako `null`, nie błąd |
| `frontend/lib/forms/validate.ts` | Jeden błąd na pole po polsku, albo brak: `validateField`, `validateCell`, `validateRowCount`, `sectionStatus` (`ready`/`inProgress`/`hasErrors`, po angielsku jako identyfikator, etykieta po polsku w `section-nav.tsx`). Oświadczenie wymaga wartości `true` wprost, bo `false` jest kompletną odpowiedzią dla `yesNo`, ale nie dla deklaracji |
| `frontend/lib/forms/validate.test.ts` | Testy walidacji: rozróżnienie "puste" od "błędne" dla pola i dla komórki tabeli, oświadczenie jawnie odznaczone, limit procentowy pola wyliczanego w procentach nie w złotówkach, pole wyliczane z przekroczonym limitem jako błąd, nie jako niedokończone |
| `frontend/lib/forms/format-computed.ts` | `formatComputedNumber`: jak pokazuje się żywa wartość pola wyliczanego, procent dla obliczenia `ratio`, kwota dla reszty. Współdzielone przez `field-input.tsx` i `table-cell.tsx`, żeby ta sama liczba nie miała dwóch formatów na jednym ekranie |

`frontend/public/` trzyma statyczne pliki (na przykład `ocwip-logo.svg`, T-07/T-15.1), poza zakresem `scripts/check_map.py` razem z resztą frontu, bo to nie kod źródłowy.

## Czego tu jeszcze nie ma

Panel recenzenta (T-40, zablokowany przez B-02), podgląd i publikacja formularza (T-27, woła ten sam `FormRenderer`), ścieżka wnioskodawcy z prawdziwym zapisem odpowiedzi (T-34, backend jeszcze nie ma endpointów wniosku, T-29). Każde ma kartę na Trello. Katalogów nie zakładamy na zapas.
