# Mapa: backend

Usługa .NET (minimal API). Warstwy i wzorce: [`../konwencje.md`](../konwencje.md).

| Plik | Co robi |
|---|---|
| `backend/Ocwip.slnx` | Solucja: projekt API plus projekt testów. Cel dla `dotnet restore` i `dotnet test` |
| `backend/src/Ocwip.Api/Ocwip.Api.csproj` | `net10.0`, nullable, `TreatWarningsAsErrors`. Pakiety: OpenAPI, EF Core 10, `Identity.EntityFrameworkCore`, Npgsql.EF, NamingConventions, Npgsql. `InternalsVisibleTo` dla `Ocwip.Api.Tests`, bo `IAccountService`/`AccountService`/`RegistrationResult` są `internal` |
| `backend/src/Ocwip.Api/Program.cs` | `grant-role` odgałęziane przed `WebApplication.CreateBuilder` (`AdminCommandLine.IsAdminInvocation`), żeby literówka w czasowniku nie wystartowała drugiego API w kontenerze. Reszta to składanie aplikacji: OpenAPI, ProblemDetails, `AppDbContext` przez `UseOcwipPostgres`, `ApplyPendingMigrations`, CORS z `Cors:Origins` (`AllowCredentials`), mapowanie endpointów (`MapEmailVerificationEndpoints`, `MapHealthEndpoints`, `MapAccountEndpoints`, `MapSessionEndpoints`, `MapCompetitionEndpoints`, `MapFormDefinitionEndpoints`, `MapApplicationEndpoints`, `MapApplicationSubmissionEndpoints`, `MapAttachmentEndpoints`, `MapPasswordResetEndpoints`) oraz na końcu `MapFallback` z `AllowAnonymous`, bo `FallbackPolicy` obejmuje też żądania niedopasowane do żadnej trasy i bez tego literówka w ścieżce dawałaby 401 zamiast 404. `MapOpenApi` też z `AllowAnonymous`, inaczej `npm run api:generate` (bez sesji) przestaje generować typy. `AddOcwipAuthentication`, `AddOcwipAuthorization` (T-13.2, domyślna odmowa) i `AddOcwipRateLimiting` (T-12.5, nie potrzebuje bazy) **poza** warunkiem na connection string, a `UseRateLimiter`/`UseAuthentication`/`UseAuthorization` między `UseCors` a mapowaniem tras, w tej kolejności: żądanie ponad limit nie ma dotykać sprawdzania hasła ani wysyłki maila, a `RequireAuthorization` na endpoincie bez zarejestrowanego schematu wywala routing całej aplikacji, nie tylko tego endpointu. `IAccountService`, `ISessionService`, `ICompetitionService`, `IFormDefinitionService`, `IApplicationService`, `IApplicationSubmissionService`, `IAttachmentService`, Identity (`AddIdentityCore<User>`, `AddIdentityConfiguration`, `AddDefaultTokenProviders`, `AddClaimsPrincipalFactory<RoleClaimsPrincipalFactory>`, `AddSignInManager`) i email (`AddDataProtection`, `IEmailSender`, `IEmailVerificationService`, `AddMemoryCache` dla throttlingu odsyłania) rejestrowane tylko, gdy jest connection string, bo `AddEntityFrameworkStores<AppDbContext>` potrzebuje `AppDbContext` żeby się zbudować, a DI musi dać się zwalidować także bez bazy. `AddDataProtection` jest konieczne osobno: `AddDefaultTokenProviders` rejestruje `DataProtectorTokenProvider`, który potrzebuje `IDataProtectionProvider`, a ASP.NET Core go sam nie dodaje. `TimeProvider.System` rejestrowany jako singleton **poza** warunkiem na connection string, bo stan konkursu jest funkcją czasu (T-20) i test musi móc go przesunąć. `IAttachmentStorage` też poza tym warunkiem (T-32): dysk lokalny nie jest sprawą bazy, a host bez connection stringa nadal musi umieć zbudować kontener DI. `public partial class Program` na końcu istnieje po to, żeby host testowy startował prawdziwą aplikację |
| `backend/src/Ocwip.Api/Configuration/IdentityConfiguration.cs` | `AddIdentityConfiguration`: polityka hasła (8 znaków, cyfra, wielka i mała litera, znak specjalny), `RequireUniqueEmail` oraz **puste** `AllowedUserNameCharacters`, bo nazwa użytkownika powtarza adres, więc domyślny filtr znaków decydowałby, jakie adresy wolno zarejestrować, plus `DataProtectionTokenProviderOptions.TokenLifespan` z `EmailVerification:TokenLifetimeHours` (domyślnie 24h, `DefaultTokenLifetimeHours`). `IConfiguration` jest opcjonalna - testy budujące `UserManager` bez własnej konfiguracji (np. `Configuration/IdentityConfigurationTests.cs`) dostają wtedy sam domyślny czas życia tokenu. Progi blokady konta (T-12.5) siedzą tutaj: `Lockout.MaxFailedAccessAttempts` z `Auth:MaxFailedLoginAttempts` (domyślnie 5), `Lockout.DefaultLockoutTimeSpan` z `Auth:LockoutMinutes` (domyślnie 15), `AllowedForNewUsers` jawnie włączone, a wartość niedodatnia w którymkolwiek z nich odmawia startu zamiast blokować wszystkich na pierwszym błędnym haśle. `SignIn.RequireConfirmedEmail` zostaje **wyłączone** także po T-12.3, i to jest decyzja, nie przeoczenie: włączone każe Identity sprawdzić potwierdzenie PRZED hasłem, więc odpowiedź różniłaby się dla adresu, który ma konto. Logowanie sprawdza je samo, po haśle, w `Services/SessionService.cs` |
| `backend/src/Ocwip.Api/Configuration/PasswordResetTokenProvider.cs` | `PasswordResetTokenProviderOptions` (własna podklasa `DataProtectionTokenProviderOptions`) oraz `PasswordResetTokenProvider<TUser>` (`DataProtectorTokenProvider<TUser>` zbudowany nad tymi opcjami). Istnieje, bo `GenerateEmailConfirmationTokenAsync` i `GeneratePasswordResetTokenAsync` dzieliłyby inaczej TEN SAM nienazwany `IOptions<DataProtectionTokenProviderOptions>`, więc jeden `TokenLifespan` rządziłby obydwoma; ten typ daje resetowi hasła (T-12.4) własną domyślną godzinę zamiast 24h weryfikacji adresu. Rejestrowany pod `IdentityConfiguration.PasswordResetTokenProviderName` w `Program.cs` |
| `backend/src/Ocwip.Api/Configuration/AuthorizationConfiguration.cs` | `AddOcwipAuthorization` (T-13.2): **jedyne miejsce z regułami dostępu**. `FallbackPolicy` wymagający zalogowania, czyli endpoint bez własnej reguły jest odmawiany przez framework, a nie otwarty dla wszystkich; polityka na każdą wartość enuma `Role`, budowana pętlą, żeby dodanie roli (R-02, administrator) było wartością w enumie, a nie przepisaniem handlerów; polityka zasobowa `resource.owner` z `EntityScopedRequirement`. Handler zasobowy rejestrowany tylko przy `hasStore`, bo czyta konto z bazy, więc host bez bazy odmawia dostępu do zasobów zamiast wywalać walidację DI |
| `backend/src/Ocwip.Api/Authorization/IEntityScoped.cs` | Znacznik zasobu należącego do Podmiotu. Dzięki niemu handler pyta o właściciela ZASÓB, a nie odkrywa tego na nowo w każdym wywołaniu, i jest napisany raz, przeciw interfejsowi, zamiast raz na klasę modelu |
| `backend/src/Ocwip.Api/Authorization/ResourceOwnership.cs` | **Jedna metoda** odpowiadająca "czy ten zasób należy do tego konta". Osobny plik, bo R-01 z `rozbieznosci.md` mówi wprost, żeby nie rozsypywać `user.EntityId` po serwisach: dziś porównanie podmiotów, po R-01 sprawdzenie członkostwa w organizacji, i to jedyne miejsce, które się wtedy zmieni. Konto bez podmiotu nie jest właścicielem niczego, jawnie, bo `EntityId` jest dziś `null` dla **każdego** konta (B-09), a po stronie zasobu jest nienullowalne, więc bez strażnika odczyt `.Value` dawałby 500 zamiast odmowy |
| `backend/src/Ocwip.Api/Authorization/EntityScopedRequirement.cs` | Wymaganie "możesz zobaczyć ten konkretny zasób". Atrybut roli tego nie wyrazi, bo dwóch wnioskodawców ma tę samą rolę i różne prawa do tego samego wniosku |
| `backend/src/Ocwip.Api/Authorization/EntityScopedHandler.cs` | Cała tabela ról dla dostępu do zasobu w jednym `switch`: operator przechodzi zawsze (widzi wszystko), wnioskodawca przez `ResourceOwnership`, recenzent (T-37) tylko dla zasobu będącego `Application`, i tylko gdy istnieje aktywny wiersz w `application_assignments` łączący jego konto z tym wnioskiem (`AppDbContext` wstrzyknięty właśnie po to), a nieznana rola i każdy zasób inny niż `Application` wpadają w odmowę. Handler nigdy nie woła `Fail`, tylko `Succeed`, więc wymaganie, którego nikt nie spełnił, jest wymaganiem odmówionym. Wiersz konta czytany raz na żądanie i zapamiętywany w polu (rejestracja `Scoped`), bo wymaganie jest sprawdzane raz na ZASÓB i lista powtarzałaby ten sam `SELECT` tyle razy, ile ma elementów |
| `backend/src/Ocwip.Api/Authorization/EvaluationAccessRequirement.cs` | "Wolno czytać albo wypełniać TĘ ocenę" (T-38), z parametrem `Write`. Osobne od wymagania zasobu podmiotu, bo ocena nie należy do żadnego podmiotu, a wnioskodawca jej nie widzi |
| `backend/src/Ocwip.Api/Authorization/EvaluationAccessHandler.cs` | Cała tabela ról dostępu do oceny (T-38) w jednym `switch`: operator czyta każdą, pisze tylko formalną; recenzent czyta i pisze wyłącznie własną kartę merytoryczną i tylko póki ma aktywne przypisanie do wniosku; reszta odmowa. Niezależność ocen dwóch ekspertów stoi tutaj |
| `backend/src/Ocwip.Api/Configuration/RateLimitingConfiguration.cs` | `AddOcwipRateLimiting` (T-12.5): jedna nazwana polityka `sensitive-auth`, licznik stałego okna partycjonowany po adresie IP (`RateLimiting:PermitLimit`/`WindowSeconds`, domyślnie 10 na 60s), wspólny budżet dla `/login`, `/register`, `/forgot-password`, `/reset-password` i `/resend-verification`. Odrzucenie to 429 z polskim komunikatem i nagłówkiem `Retry-After`, nie 500 i nie cichy przepust. Odmawia startu przy limicie lub oknie mniejszym lub równym zeru, tym samym powodem co `AuthenticationConfiguration` dla `Auth:SessionLifetimeHours` |
| `backend/src/Ocwip.Api/Configuration/AuthenticationConfiguration.cs` | `AddOcwipAuthentication`: ciasteczko sesyjne `ocwip.session` na schemacie `IdentityConstants.ApplicationScheme`. HttpOnly, `SameSite` z `Auth:CookieSameSite` (domyślnie `Lax`, bo `Strict` nie dołącza ciasteczka do pierwszego żądania po kliknięciu w link z maila), `Secure` z `Auth:SecureCookie` (domyślnie wszędzie poza Development, bo lokalny stack stoi na http i przeglądarka wyrzuciłaby ciasteczko), czas życia z `Auth:SessionLifetimeHours` (`DefaultSessionLifetimeHours`, 8 h, sliding). `AddIdentityCookies`, a nie sam cookie handler, bo `SignInManager.SignOutAsync` wylogowuje z trzech schematów po nazwie i brakujący schemat rzuca wyjątkiem. `OnRedirectToLogin`/`OnRedirectToAccessDenied` zamienione na 401/403 w `problem+json`, bo domyślne 302 na `/Account/Login` daje fetchowi 200 i stronę logowania zamiast błędu. Odmawia startu przy `Auth:SessionLifetimeHours` <= 0 (ciasteczko wygasłe w chwili wydania) i przy `SameSite=None` bez `Secure` (przeglądarka je wyrzuca bez słowa). Przy obecnej bazie (`hasStore`) dokłada `SecurityStampValidatorOptions.ValidationInterval = Zero` i podmienia `ISecurityStampValidator` na `ActiveAccountStampValidator` przez `AddScoped`, **nie** `TryAddScoped`: Identity rejestruje swój własny, więc `TryAdd` jest cichym brakiem operacji. Bez bazy nadpisuje `OnValidatePrincipal` na bezwarunkową odmowę, bo domyślny woła `GetRequiredService<ISecurityStampValidator>` i ciasteczko, które ten pierścień kluczy jeszcze odszyfruje, dałoby 500 zamiast 401 |
| `backend/src/Ocwip.Api/Configuration/CustomPasswordErrorConfiguration.cs` | `IdentityErrorDescriber` z polskimi komunikatami pięciu błędów odrzuconego hasła (`PasswordTooShort`, `PasswordRequiresDigit`, `PasswordRequiresUpper`, `PasswordRequiresLower`, `PasswordRequiresNonAlphanumeric`). Długość czytana z parametru, nie wpisana na sztywno, żeby zmiana `RequiredLength` nie zostawiła komunikatu ze starą liczbą |
| `backend/src/Ocwip.Api/Contracts/RegisterRequest.cs` | Rekord `RegisterRequest(Email, Password, FirstName, LastName, ReturnUrl?)`, ciało `POST /register`. `ReturnUrl` (T-12.8) to nie dana od wnioskodawcy, tylko strona konkursu, z której przyszedł, niesiona przez mail weryfikacyjny. **Bez PESEL-u i bez podmiotu**, oba świadomie: PESEL pojawia się na etapie umowy, a zakładanie Podmiotu czeka na B-09. `ToString` nadpisany i nie drukuje nic poza nazwą typu, bo domyślny `ToString` rekordu drukuje każdą właściwość, więc i hasło (reguła 4) |
| `backend/src/Ocwip.Api/Contracts/LoginRequest.cs` | Rekord `LoginRequest(Email, Password, ReturnUrl?)`, ciało `POST /login`. `ReturnUrl` to reguła z raportu (krok 3.1): wnioskodawca wraca na stronę konkursu, z której przyszedł. Proponuje ją przeglądarka, rozstrzyga serwer, patrz `Services/LoginLandingPath.cs`. `ToString` nadpisany z tego samego powodu co w `RegisterRequest.cs` |
| `backend/src/Ocwip.Api/Contracts/LoginResponse.cs` | Rekord `LoginResponse(Email, FirstName, LastName, Role, RedirectPath)`, ciało udanego logowania. Sesja jedzie w ciasteczku, więc nic tutaj nie jest poświadczeniem |
| `backend/src/Ocwip.Api/Contracts/CompetitionRequest.cs` | Ciało tworzenia i edycji konkursu, jeden rekord na oba. Świadomie **bez statusu**: stan zmienia się wyłącznie przez tabelę przejść. Parametry z kroków 1.2 do 1.6 kreatora to T-20a |
| `backend/src/Ocwip.Api/Contracts/CompetitionAttachmentRequest.cs` | Jeden wymagany załącznik konkursu w żądaniu operatora (krok 1.5). Bez identyfikatora: lista przyjeżdża w całości i zastępuje zapisaną, bo tak edytuje ją kreator |
| `backend/src/Ocwip.Api/Contracts/CompetitionAttachmentResponse.cs` | Ten sam załącznik w odczycie, także publicznym: wnioskodawca musi wiedzieć, co przygotować, zanim założy konto |
| `backend/src/Ocwip.Api/Contracts/CompetitionContactResponse.cs` | Osoba kontaktowa konkursu: imię i nazwisko oraz służbowy adres, publikowane świadomie (krok 1.6). Nic więcej z konta tędy nie jedzie |
| `backend/src/Ocwip.Api/Contracts/CompetitionRequestValidator.cs` | Walidacja kształtu żądania na brzegu API, po polsku: limity długości powtórzone z konfiguracji EF (inaczej za długi tytuł wraca jako 500), kwota dodatnia, mieszcząca się w `numeric(18,2)` i bez cichego zaokrąglenia (test przez `decimal.Round`, nie przez liczenie cyfr), sparowanie `endDate` z naborem ciągłym w obie strony oraz porównanie dat **po ucięciu do pełnej minuty** |
| `backend/src/Ocwip.Api/Contracts/CompetitionStatusChangeRequest.cs` | Prośba o jeden ruch w cyklu życia. Jeden endpoint ze stanem docelowym zamiast trasy na czasownik, bo tabela przejść jest już autorytetem |
| `backend/src/Ocwip.Api/Contracts/CompetitionResponse.cs` | Konkurs oczami operatora: stan **efektywny**, `allowedTransitions` prosto z tabeli przejść (panel rysuje przyciski z reguły, nie z jej kopii), znaczniki audytowe |
| `backend/src/Ocwip.Api/Contracts/CompetitionIntakeResponse.cs` | Stan naboru po drucie plus `CompetitionIntakeMessage`: polski komunikat z **wyliczoną wartością graniczną** (D12), czyli datą i godziną zamknięcia w czasie polskim. Jedyne miejsce, w którym instant UTC zamienia się w zegar ścienny; bez bazy stref czasowych komunikat mówi wprost "czasu UTC" zamiast podać godzinę o jedną obok |
| `backend/src/Ocwip.Api/Contracts/PublicCompetitionResponse.cs` | Konkurs oczami gościa. Osobny typ, nie ten sam z pustymi polami: typ, który nie potrafi nieść śladu audytowego, nie może go wyciec |
| `backend/src/Ocwip.Api/Contracts/FormDefinitionRequest.cs` | Publikacja jednej wersji formularza: w ciele jest sam dokument. Numeru wersji w żądaniu **nie ma**, bo numer jest licznikiem tego, co już opublikowano, a podanie go z zewnątrz byłoby jedyną drogą do nadpisania wersji, na którą wskazują wnioski |
| `backend/src/Ocwip.Api/Contracts/FormDefinitionResponse.cs` | Wersja formularza z dokumentem plus `FormDefinitionSummaryResponse` bez dokumentu na listę wersji. Dwa typy, nie jeden z nullem, bo dokument jest tą dużą częścią. `IsCurrent` mówi wprost, która wersja jest w mocy, zamiast kazać ekranowi liczyć to z dwóch odpowiedzi |
| `backend/src/Ocwip.Api/Contracts/ApplicationResponse.cs` | Wniosek, roboczy albo złożony, oczami jego właściciela albo operatora (T-29): `Checksum` (D15) liczony na nowo przy każdym odczycie przez `Models/ApplicationChecksum.cs`, nigdy zapisywany, `LastSavedAt` to `UpdatedAt` wiersza, żeby front pokazał "zapisano o 14:32" bez własnego zegara. Ten sam rekord wraca też z `POST /applications/{id}/submit` (T-33), bez osobnego kontraktu |
| `backend/src/Ocwip.Api/Contracts/ApplicationListResponse.cs` | Lista złożonych wniosków konkursu oczami operatora (T-35): numer, podmiot i jego rodzaj, tytuł, koszt i wnioskowana kwota z ról pól formularza (null, gdy formularz ich nie oznacza), suma wnioskowanych kwot i to, co zostało z puli (null bez ustawionej puli) |
| `backend/src/Ocwip.Api/Contracts/SubmittedApplicationResponse.cs` | Jedna złożona oferta dla operatora (T-35): odpowiedzi razem z definicją wersji formularza, na której ją wypełniono, suma kontrolna D15 i aktywne załączniki |
| `backend/src/Ocwip.Api/Contracts/ApplicationFormResponse.cs` | Dokument formularza, na którym stoi wniosek, przy wersji przypiętej do wniosku, nie najnowszej (T-34). Osobno od `ApplicationResponse`, żeby autozapis nie powtarzał tego samego dokumentu przy każdym zapisanym polu |
| `backend/src/Ocwip.Api/Contracts/ApplicationOverviewResponse.cs` | Jeden wiersz "Moje wnioski" oczami wnioskodawcy (T-34): konkurs, status, numer i daty, bez odpowiedzi, lżejszy niż `ApplicationResponse` |
| `backend/src/Ocwip.Api/Contracts/AttachmentResponse.cs` | Metadane jednego załącznika (T-32). Nigdy bajty: te wracają z `GET /attachments/{id}` jako plik, nie jako JSON |
| `backend/src/Ocwip.Api/Contracts/SaveApplicationDraftRequest.cs` | Ciało autozapisu (T-29): cały komplet odpowiedzi na raz, `JsonElement` jak `FormDefinitionRequest.Definition`, bo szkic wniosku świadomie dopuszcza braki (T-30 dopiero pilnuje kompletu, przy złożeniu) |
| `backend/src/Ocwip.Api/Contracts/AssignReviewerRequest.cs` | Ciało `POST /applications/{id}/assignments` (T-37): sam identyfikator recenzenta, wniosek jedzie w trasie, ten sam podział co konkurs przy tworzeniu szkicu |
| `backend/src/Ocwip.Api/Contracts/ApplicationAssignmentResponse.cs` | Jedno przypisanie recenzenta do wniosku (T-37): identyfikator, wniosek, recenzent, `IsActive`. Wspólny kształt dla przypisania i cofnięcia |
| `backend/src/Ocwip.Api/Contracts/EvaluationResponse.cs` | Ocena z dokumentem karty (ekspert nie czyta tras kart operatora) i wynikiem odczytanym z odpowiedzi: wynik formalny, suma merytoryczna, suma strategiczna, kwota rekomendowana. Plus `SaveEvaluationRequest`, autozapis całego kompletu odpowiedzi |
| `backend/src/Ocwip.Api/Contracts/CurrentUserResponse.cs` | Rekord `CurrentUserResponse(Id, Email, FirstName, LastName, Role)`, ciało `GET /me`. Celowo chudy: bez tego endpointu unieważniona sesja wygląda z zewnątrz identycznie jak żywa |
| `backend/src/Ocwip.Api/Contracts/RegisterRequestValidator.cs` | Kształt żądania sprawdzany na brzegu API, komunikaty po polsku. `Trim` obcina biały znak z adresu i imion (nie z hasła) ZANIM cokolwiek je przeczyta, bo wersja nieobcięta i tak trafiłaby do `normalized_email`. Adres przez `MailAddress.TryCreate`, porównanie w obie strony (forma `Adam <adam@x.pl>`) oraz `EmailAddressAttribute`, bo dokładnie ten trzeci sprawdzian robi walidator Identity i bez niego jego angielski `InvalidEmail` wraca pod polem hasła. Hasło tylko na obecność, bo polityka żyje w `IdentityConfiguration.cs`. Znaki sterujące i formatujące (kategorie Unicode Cc i Cf) odrzucane w adresie i w imionach, bo `Trim` sięga tylko na końce wartości: `Adam\r\nBcc: ...` zapisywał się jak najęty, a `MailAddress` nie widzi znaku zerowej szerokości, więc jeden widoczny adres brał dwa konta |
| `backend/src/Ocwip.Api/Contracts/IEmailSender.cs` | Kontrakt `SendAsync(EmailMessage, CancellationToken)`, wdrażany przez `EmailSenderService.cs` (produkcja) i `RecordingEmailSender` w testach |
| `backend/src/Ocwip.Api/Contracts/VerifyEmailRequest.cs` | Rekord `VerifyEmailRequest(UserId, Token)`, ciało `POST /verify-email` |
| `backend/src/Ocwip.Api/Contracts/ResendVerificationRequest.cs` | Rekord `ResendVerificationRequest(Email, ReturnUrl?)`, ciało `POST /resend-verification`; `ReturnUrl` jak w `RegisterRequest.cs`, żeby nowy link prowadził tam, gdzie wygasły |
| `backend/src/Ocwip.Api/Contracts/ForgotPasswordRequest.cs` | Rekord `ForgotPasswordRequest(Email)`, ciało `POST /forgot-password` |
| `backend/src/Ocwip.Api/Contracts/ResetPasswordRequest.cs` | Rekord `ResetPasswordRequest(UserId, Token, NewPassword)`, ciało `POST /reset-password`. `ToString` nadpisany z tego samego powodu co w `RegisterRequest.cs`, bo niesie hasło |
| `backend/src/Ocwip.Api/Contracts/HealthResponse.cs` | Rekord `HealthResponse(Status)`, ciało `GET /health`. Nazwany po to, żeby dokument OpenAPI miał co opisać: typ anonimowy nie da się wpisać w sygnaturę endpointu |
| `backend/src/Ocwip.Api/Contracts/DatabaseHealthResponse.cs` | Rekord `DatabaseHealthResponse(Status, Database)`, ciało `GET /health/db` przy udanej sondzie |
| `backend/src/Ocwip.Api/Models/EmailMessage.cs` | Rekord `EmailMessage(To, Subject, Body)`, jedyny kształt wiadomości, jaki zna `IEmailSender` |
| `backend/src/Ocwip.Api/Services/IAccountService.cs` | `RegistrationOutcome` i `RegistrationResult` plus kontrakt `RegisterAsync`. **Nie zwraca `IdentityResult`**: `Accepted` znaczy jednocześnie "konto utworzone" i "adres zajęty", więc różnicy nie da się zwrócić, patrz decyzja o rejestracji w [`architektura.md`](../architektura.md) |
| `backend/src/Ocwip.Api/Services/ISessionService.cs` | `LoginOutcome` (`Succeeded`, `InvalidCredentials`, `EmailNotConfirmed`), `LoginResult` i kontrakt `LoginAsync`/`LogoutAsync`/`CurrentUserAsync`. Jedna wartość na wszystkie powody, których wołający nie może poznać (nie ma konta, złe hasło, konto nieaktywne), więc nie ma gałęzi do pomylenia. `EmailNotConfirmed` jest osiągalne **wyłącznie po poprawnym haśle**, i to godzi czytelny komunikat z regułą 3 |
| `backend/src/Ocwip.Api/Services/SessionService.cs` | `LoginAsync` przez `CheckPasswordSignInAsync`, a nie `PasswordSignInAsync`, bo to drugie sprawdza potwierdzenie adresu przed hasłem. Kolejność: hasło, `IsActive`, `EmailConfirmed`, dopiero `SignInAsync`. Dla nieistniejącego adresu liczy hash atrapy (`DecoyHash`), żeby czas odpowiedzi nie rozróżniał kont. `lockoutOnFailure: false`, bo liczenie prób to T-12.5. `LogoutAsync` obraca `SecurityStamp` przed `SignOutAsync`: samo skasowanie ciasteczka nie unieważnia kopii zabranej ze współdzielonego komputera, więc wylogowanie kończy **wszystkie** sesje konta. `CurrentUserAsync` czyta wiersz, nie tylko bilet, więc konto wyłączone w trakcie sesji przestaje wchodzić |
| `backend/src/Ocwip.Api/Services/ICompetitionService.cs` | Kontrakt operacji na konkursie plus `CompetitionOutcome` (jedna wartość na jeden kod odpowiedzi: `NotFound`, `NumberTaken`, `TransitionNotAllowed`, `UnknownFormDefinition`, `Inactive`) i `CompetitionResult` |
| `backend/src/Ocwip.Api/Services/CompetitionService.cs` | Cały cykl życia konkursu. **Nie porównuje statusów sam**: pyta `CompetitionStatusTransitions` i `CompetitionLifecycle`. Tworzenie zawsze daje szkic, przejścia liczone od stanu efektywnego, publikacja stempluje `PublishedAt`, dezaktywacja jest idempotentna i miękka (i zeruje `allowedTransitions`, żeby panel nie rysował przycisku dającego 409), zajętość numeru liczy się tylko wśród aktywnych i ma wyłapany wyścig na `23505` jak w `AccountService`, lista publiczna filtruje po statusie zapisanym (bo zegar nigdy nie produkuje `Draft` ani `Archived`). Zegar wstrzykiwany jako `TimeProvider`. `FormDefinitionId` przepisywane **tylko wtedy, gdy żądanie je niesie**: null znaczy "nie zmieniam wersji formularza", bo od T-25 kolumnę ustawia publikacja, a edycja terminów bez tego pola cofałaby konkurs do stanu bez formularza |
| `backend/src/Ocwip.Api/Services/IFormDefinitionService.cs` | Kontrakt wersjonowania formularza plus `FormDefinitionOutcome` (`NotFound`, `Inactive`, `InvalidDefinition`, `VersionTaken`) i `FormDefinitionResult`. Kształt interfejsu **jest** kartą T-25: jest publikacja i dwa odczyty, nie ma zmiany ani kasowania wersji |
| `backend/src/Ocwip.Api/Services/FormDefinitionService.cs` | Wersjonowanie definicji formularza (T-25): publikacja **dokłada wiersz**, nigdy nie nadpisuje. Kolejny numer to `max + 1` liczone razem z wersjami nieaktywnymi (numer raz zużyty nie wraca), dokument przechodzi przez bramkę `FormSchemaValidator`, nowa wersja staje się wersją w mocy dla konkursu, a wnioski nie są ruszane. Wyścig dwóch publikacji wyłapany na nazwanym indeksie jak numer w `CompetitionService`. Identyfikator wersji nadawany w kodzie, bo konkurs wskazuje na nią w tym samym zapisie |
| `backend/src/Ocwip.Api/Services/IApplicationService.cs` | Kontrakt wersji roboczej wniosku (T-29) plus `ApplicationOutcome` (`CompetitionNotFound`, `NoFormDefinition`, `IntakeClosed`, `NoEntity`, `NotFound`, `AlreadySubmitted`, `InvalidAnswers`), `ApplicationResult` i `ApplicationFormResult` (T-34). `FindForAuthorizationAsync` osobno od reszty, bo odmowa autoryzacji zapada PRZED wywołaniem serwisu, nie jest jednym z wyników |
| `backend/src/Ocwip.Api/Services/ApplicationService.cs` | Wersja robocza wniosku (T-29): tworzenie pyta `CompetitionIntake` (T-21) i sprawdza, czy konkurs ma opublikowany formularz, zapis odrzuca kształt inny niż obiekt albo tablica oraz wniosek, który przestał być szkicem, usunięcie tylko oznacza `IsActive = false`. Każdy zapis kończy się `Entry(...).ReloadAsync`, bo `timestamptz` w PostgreSQL ma precyzję mikrosekundy, o jedną cyfrę mniej niż tick .NET, a suma kontrolna (D15) musi liczyć z tej samej wartości `UpdatedAt`, którą odda kolejny odczyt. `GetFormDefinitionAsync` (T-34) czyta dokument z `FormDefinition` przypiętej do wniosku, nie z najnowszej wersji konkursu |
| `backend/src/Ocwip.Api/Services/IApplicationSubmissionService.cs` | Kontrakt złożenia (T-33) plus `ApplicationSubmissionOutcome` i `ApplicationSubmissionResult`/`ApplicationConfirmationPdfResult`, osobny enum od `IApplicationService.cs` mimo powtarzających się wartości: złożenie jest osobnym modułem od autozapisu wersji roboczej |
| `backend/src/Ocwip.Api/Services/ApplicationSubmissionService.cs` | Złożenie oferty (T-33): waliduje na poziomie złożenia (`AnswerStrictness.Submission`), pyta `CompetitionIntake`, sprawdza obronnie zgodność pary konkurs/definicja formularza (rzuca wyjątkiem, gdy się rozjadą, bo dzisiejsze ścieżki zapisu nie potrafią tego wyprodukować same), oddaje nadanie numeru i zmianę statusu do `ApplicationNumberAssigner`, wysyła e-mail potwierdzający przez `IEmailSender` i generuje PDF potwierdzenia przez `Services/Pdf` |
| `backend/src/Ocwip.Api/Services/IApplicationListService.cs` | Kontrakt odczytu listy wniosków i jednej złożonej oferty (T-35). Szkic, wniosek wycofany i wniosek innego konkursu dają null, bez rozróżniania |
| `backend/src/Ocwip.Api/Services/ApplicationListService.cs` | Lista złożonych i aktywnych wniosków konkursu ("nie szkic" zamiast "Submitted", żeby stany z M5 zostały na liście), kolejność po numerze liczbowo, jedno zapytanie i jedna analiza definicji na wersję formularza (bez `Include`, żeby JSONB nie szedł przy każdym wierszu), wartości przez `ApplicationRoleValues`; oferta z aktywnymi załącznikami |
| `backend/src/Ocwip.Api/Services/IApplicationOverviewService.cs` | Kontrakt "Moje wnioski" wnioskodawcy (T-34) plus `ApplicationOverviewOutcome`/`ApplicationOverviewResult`, osobny od `IApplicationService.cs`, który jest celowo wąski do potrzeb szkicu |
| `backend/src/Ocwip.Api/Services/ApplicationOverviewService.cs` | Wnioski wywołującego przez wszystkie konkursy naraz (T-34): szkic i złożony razem, filtr po `EntityId` z konta i po `IsActive`, żeby dezaktywowany szkic zniknął z listy zgodnie z obietnicą z T-29, kolejność po ostatnim zapisie |
| `backend/src/Ocwip.Api/Services/IApplicationAssignmentService.cs` | Kontrakt przypisania i cofnięcia (T-37) plus `ApplicationAssignmentOutcome` (`Succeeded`, `ApplicationNotFound`, `ReviewerNotFound`, `NotAssigned`) i `ApplicationAssignmentResult`. Nie decyduje, kto co widzi: tę regułę czyta `EntityScopedHandler.cs` z tej samej tabeli, ten interfejs tylko ją zapisuje |
| `backend/src/Ocwip.Api/Services/ApplicationAssignmentService.cs` | Przypisanie (T-37): sprawdza istnienie wniosku i aktywnego konta recenzenta (rola `Reviewer`, `IsActive`), potem albo wstawia wiersz, albo reaktywuje wcześniej cofnięty (jeden wiersz na parę wniosek plus recenzent, unikalny indeks). Cofnięcie nigdy nie kasuje wiersza, tylko ustawia `IsActive = false`, idempotentnie jak `ApplicationService.DeactivateAsync` |
| `backend/src/Ocwip.Api/Services/IEvaluationService.cs` | Kontrakt ocen (T-38) plus `EvaluationOutcome` (`Created` osobno od `Succeeded`, `NotSubmitted`, `NoCard`, `AlreadyFinished`, `AnswersRejected`) i `EvaluationResult`. Kto może co, rozstrzyga warstwa autoryzacji, nie serwis |
| `backend/src/Ocwip.Api/Services/EvaluationService.cs` | Oceny (T-38): otwarcie karty (jedna formalna na wniosek, jedna merytoryczna na parę wniosek plus ekspert, wyścig dwóch kart rozstrzyga indeks częściowy), zapis szkicu i zakończenie etapu przez `AnswerValidator` z rodzajem wnioskodawcy, na wersji karty zamrożonej przy otwarciu. Wynik zawsze liczony `EvaluationScores`, nigdy zapisywany |
| `backend/src/Ocwip.Api/Services/ApplicationNumberAssigner.cs` | Nadanie numeru wniosku pod blokadą doradczą `pg_advisory_xact_lock` na konkurs, w jednej transakcji z przestawieniem statusu i dopisaniem wpisu do `ApplicationStatusHistory` (T-33). Po wzięciu blokady odczytuje status i `IsActive` NA NOWO wprost z bazy, zamiast ufać kopii, którą wołający wczytał przed czekaniem na blokadę: dwa złożenia TEGO SAMEGO wniosku współdzielą tę samą blokadę konkursu, więc drugie dociera tu dopiero po zatwierdzeniu pierwszego, a jego własna kopia w pamięci jest wtedy już nieaktualna. Kolejny numer liczony po stronie SQL (`MAX(number::integer)`), nie w pamięci, żeby nie wydłużać czasu trzymania blokady. Uzasadnienie strategii numeracji w `architektura.md` |
| `backend/src/Ocwip.Api/Services/Pdf/SimplePdfDocument.cs` | Ręcznie pisany PDF (fonty Base14, `WinAnsiEncoding`) bez żadnej biblioteki: liczy własne przesunięcia bajtowe obiektów i buduje tabelę xref. Jedna strona pionowa dla potwierdzenia (T-33) albo tyle stron, ile trzeba, w układzie `PdfPageLayout` z nagłówkiem powtarzanym na każdej (T-35). Przyjmuje wyłącznie ASCII |
| `backend/src/Ocwip.Api/Services/Pdf/ApplicationConfirmationPdfBuilder.cs` | Treść potwierdzenia złożenia (T-33): numer wniosku, konkurs, wersja formularza, data złożenia, suma kontrolna, przepuszczone przez `PdfText.Transliterate` |
| `backend/src/Ocwip.Api/Services/Pdf/ApplicationListPdfBuilder.cs` | Lista wniosków jako PDF (T-35): poziome A4, Courier, kolumny dopełniane spacjami, tekst dłuższy niż kolumna ucięty wielokropkiem (z tytułem konkursu włącznie), nagłówek na każdej stronie, sumy na końcu; własna, krótsza nazwa rodzaju dla grupy pod patronatem, bo pełna nie mieści się w kolumnie |
| `backend/src/Ocwip.Api/Services/Pdf/PdfText.cs` | Transliteracja polskich znaków na ASCII dla `SimplePdfDocument` (fonty Base14 nie mają ich w `WinAnsiEncoding`), wspólna dla obu PDF-ów; inny znak spoza ASCII staje się `?`. Zawężenie dotyczy tylko PDF-ów |
| `backend/src/Ocwip.Api/Services/Export/ApplicationListCsv.cs` | Lista wniosków jako CSV (T-35): UTF-8 z BOM, średnik, CRLF, żeby polski Excel otworzył ją bez okna importu. Komórka zaczynająca się jak formuła dostaje apostrof (wstrzyknięcie do arkusza), zwykła liczba nie |
| `backend/src/Ocwip.Api/Services/Export/ApplicationListLabels.cs` | Polskie nazwy kolumn, rodzajów wnioskodawcy i statusów dla obu eksportów (rodzaj w PDF-ie nadpisuje `ApplicationListPdfBuilder`), kwota z przecinkiem i dwoma miejscami pisana ręcznie (bez kultury `pl-PL`, jak `PolishNumbers`), data w czasie polskim albo jawnie w UTC |
| `backend/src/Ocwip.Api/Services/AttachmentFormatDetector.cs` | Decyduje, który z ośmiu dozwolonych formatów niesie plik, z sygnatury bajtów i rozszerzenia nazwy, nigdy z deklarowanego `Content-Type` (T-32). `CanonicalContentType` daje nagłówek odpowiedzi pobrania z wykrytego formatu, nie z deklaracji |
| `backend/src/Ocwip.Api/Services/IAttachmentStorage.cs` | Kontrakt zapisu i odczytu bajtów załącznika, osobno od metadanych w bazie (T-32) |
| `backend/src/Ocwip.Api/Services/AttachmentStorageService.cs` | Dysk lokalny pod `Attachments:StoragePath`: każdy plik jako goły GUID bez rozszerzenia i bez śladu oryginalnej nazwy, nigdy nie kasowany (reguła 5, "poprzedni nie znika twardo") |
| `backend/src/Ocwip.Api/Services/IAttachmentService.cs` | Kontrakt przesyłania, podmiany, pobierania i listowania załączników plus `AttachmentOutcome` i `AttachmentResult` (T-32, `ListAsync` z T-34). `FindForAuthorizationAsync` jak w `IApplicationService.cs`, z tego samego powodu |
| `backend/src/Ocwip.Api/Services/AttachmentService.cs` | Przesyłanie (T-32): te same trzy strażniki co `ApplicationService` (nabór z `CompetitionIntake`, status szkicu, `IsActive`, R-29), czytanie w kawałkach do limitu na plik zamiast ufania `Content-Length`, suma aktywnych załączników wniosku względem limitu konkursu, format z `AttachmentFormatDetector`. Podmiana wstawia nowy wiersz i dezaktywuje poprzedni, nigdy go nie kasując. `ListAsync` (T-34): aktywne załączniki jednego wniosku, od najstarszego |
| `backend/src/Ocwip.Api/Services/LoginLandingPath.cs` | Dokąd trafia sesja po zalogowaniu: `/panel/operator`, `/panel/applicant`, `/panel/reviewer` (panele powstają w T-15.2 i T-15.3). `For` mapuje rolę, `Resolve` przyjmuje propozycję wołającego tylko wtedy, gdy jest jedną lokalną ścieżką: wszystko inne (adres bezwzględny, `//`, backslash, znak sterujący, ścieżka względna) jest **odrzucane, a nie naprawiane**, bo sanitizer to lista sztuczek, o których ktoś już pomyślał. Nieznana rola spada na panel wnioskodawcy, nie operatora. `SafeOrNull` to ta sama reguła osobno (T-12.8): zwraca przyciętą ścieżkę albo null, odrzuca też dłuższe niż `MaxLength` (512), używa jej mail weryfikacyjny |
| `backend/src/Ocwip.Api/Services/ActiveAccountStampValidator.cs` | `SecurityStampValidator<User>` plus jedna reguła, której Identity nie zna: konto nieaktywne zostaje wylogowane. W potoku ciasteczka, a nie w endpoincie, bo nie kasujemy twardo (reguła 5), więc wyłączenie konta to jedyne "usunięcie", jakie mamy, a ciasteczko wydane wcześniej pozostaje kryptograficznie poprawne. Sprawdzenie wpisane w jeden endpoint chroni jeden endpoint, a T-13.2 zaraz doda ich wiele |
| `backend/src/Ocwip.Api/Services/RoleClaimsPrincipalFactory.cs` | Przepisuje kolumnę `role` do claimu `ClaimTypes.Role`. Bez tego principal nie ma roli w ogóle, bo tabel `AspNetRoles` tu nie ma i fabryka Identity nie ma czego czytać, a polityki z T-13.2 po cichu odmawiałyby wszystkiego. Standardowy typ claimu, żeby `IsInRole` i polityki działały bez warstwy tłumaczącej |
| `backend/src/Ocwip.Api/Services/AccountService.cs` | `RegisterAsync` przez `UserManager<User>`, z `ThrowIfCancellationRequested` na wejściu, bo `UserManager` nie ma przeciążenia z tokenem, a przyjęty i zignorowany token zapisuje konto komuś, kto już się rozłączył. Obsługuje OBIE drogi do zajętego adresu: `DuplicateEmail`/`DuplicateUserName` z walidatora i SQLSTATE 23505 na `ix_users_normalized_email`, gdy walidator przegra wyścig; żadna z nich nie woła `IEmailVerificationService`. Kody duplikatu są odfiltrowywane, a nie raportowane. Dopiero po realnym sukcesie tworzenia konta woła `IEmailVerificationService.SendVerificationAsync`. Roli ani `EmailConfirmed` nie ustawia: żaden endpoint nie może wybierać roli |
| `backend/src/Ocwip.Api/Services/IEmailVerificationService.cs` | Kontrakt: `SendVerificationAsync(User, returnUrl?)`, `VerifyAsync(userId, encodedToken)`, `ResendVerificationAsync(email, returnUrl?)` |
| `backend/src/Ocwip.Api/Services/EmailVerificationService.cs` | `SendVerificationAsync` generuje token przez `UserManager.GenerateEmailConfirmationTokenAsync`, koduje go Base64Url i buduje link do `EmailVerification:FrontendBaseUrl` (domyślnie `http://localhost:3000`) `/verify-email?userId=&token=`, z `&returnUrl=` tylko wtedy, gdy przejdzie `LoginLandingPath.SafeOrNull` (T-12.8; inny jest pomijany, nie naprawiany). `VerifyAsync` woła `ConfirmEmailAsync` - `EmailConfirmed` z Identity jest jedynym źródłem prawdy o weryfikacji, nic tu nie synchronizuje drugiego pola. `ResendVerificationAsync` zawsze zwraca `true` (konto nieznane, już potwierdzone i wciąż w throttlingu wyglądają dla wywołującego identycznie, celowo). Throttling odsyłania to rosnący cooldown w `IMemoryCache` (`ResendBackoffCalculator`): baza `EmailVerification:ResendCooldownSeconds` (domyślnie 60s) rośnie razy `EmailVerification:ResendCooldownMultiplier` (domyślnie 5) z każdą kolejną wysyłką, do sufitu `EmailVerification:ResendCooldownMaxSeconds` (domyślnie 1800s), i wraca do bazy po `EmailVerification:ResendCooldownResetSeconds` ciszy (domyślnie 24h). Tylko w pamięci procesu - przy skalowaniu do wielu instancji potrzebuje wspólnego magazynu. `VerifyAsync` odrzuca identyfikator, który nie jest GUID-em, zanim dotknie Identity, tak samo jak nieznany (T-51) |
| `backend/src/Ocwip.Api/Services/IPasswordResetService.cs` | `PasswordResetOutcome` (`Succeeded`, `InvalidToken`, `PasswordRejected`) i `PasswordResetResult` plus kontrakt `RequestResetAsync`/`ResetAsync` (T-12.4). `InvalidToken` łączy nieznane konto, token uszkodzony, wygasły i już użyty w jedną wartość, tym samym powodem co `LoginOutcome` łączy nieznane konto i złe hasło |
| `backend/src/Ocwip.Api/Services/PasswordResetService.cs` | `RequestResetAsync` zawsze zwraca sukces wołającemu (jak `ResendVerificationAsync`, brak enumeracji kont), generuje token przez `UserManager.GeneratePasswordResetTokenAsync` (idzie przez `PasswordResetTokenProvider`, nie przez "Default"), koduje Base64Url, link do `/reset-password`. `ResetAsync` woła `UserManager.ResetPasswordAsync`: błąd `nameof(IdentityErrorDescriber.InvalidToken)` mapowany na `PasswordResetOutcome.InvalidToken`, każdy inny błąd (polityka hasła) na `PasswordRejected`. Unieważnienie wszystkich sesji dzieje się samo: `ResetPasswordAsync` obraca `SecurityStamp` przy sukcesie, tym samym mechanizmem co wylogowanie w `SessionService.cs` |
| `backend/src/Ocwip.Api/Services/ResendBackoffCalculator.cs` | Czysta matematyka rosnącego cooldownu (`CooldownFor`), wydzielona z `EmailVerificationService`, żeby dało się ją testować bez cache'a, bazy i prawdziwego czekania - zob. `ResendBackoffCalculatorTests.cs` |
| `backend/src/Ocwip.Api/Services/EmailSenderService.cs` | `IEmailSender` na produkcję: wyłącznie loguje treść maila (`ILogger`), nie wysyła nic naprawdę - świadoma decyzja z pierwszej wersji weryfikacji, prawdziwy dostawca SMTP/e-mail zostaje osobną kartą |
| `backend/src/Ocwip.Api/Endpoints/AccountEndpoints.cs` | `MapAccountEndpoints`: `GET /accounts/operators` (T-22, tylko operator: lista aktywnych kont z rolą Operator do wyboru osoby kontaktowej w kroku 1.6 kreatora, 503 bez bazy) oraz `POST /register`. Walidacja wejścia, potem jedna i ta sama odpowiedź **202 z pustym ciałem** dla adresu zajętego i wolnego, oraz 400 z `ProblemDetails` gdy hasło nie przeszło polityki. `accounts`/`directory` jako `[FromServices]` jawnie, bo obie usługi są zarejestrowane warunkowo, a bez tego minimalne API rozstrzyga źródło parametru przy budowie WSZYSTKICH tras i na hoście bez bazy wywala routing. Typ jest **nullowalny**, więc binder woła `GetService`, a nie `GetRequiredService`: host bez bazy dostaje 503 z ogólnym komunikatem, a nie 500 z nazwą wewnętrznego typu w ciele. `MapEmailVerificationEndpoints` w tym samym pliku: `POST /verify-email` (200 albo 400 z `ProblemDetails` i komunikatem po polsku o niepoprawnym/wygasłym/już potwierdzonym linku) i `POST /resend-verification` (zawsze 200, patrz `EmailVerificationService.cs`). Wszystkie trasy zwracają `TypedResults` z jawnym typem w sygnaturze (`Results<...>`), więc kody odpowiedzi w dokumencie OpenAPI pilnuje kompilator. Wyjątkiem jest `ProblemHttpResult`, którego status siedzi w argumencie czasu wykonania: 503 na `/register` i 400 na `/verify-email` deklarowane ręcznie przez `.ProducesProblem`, nie `.Produces<ProblemDetails>`, bo to drugie deklaruje `application/json`, a endpoint wysyła `application/problem+json` |
| `backend/src/Ocwip.Api/Contracts/OperatorAccountResponse.cs` | Jedno konto pracownika OCWIP (T-22, krok 1.6): identyfikator, imię, nazwisko, adres. Nic więcej z konta tędy nie jedzie, tak jak `CompetitionContactResponse.cs` |
| `backend/src/Ocwip.Api/Services/IOperatorDirectoryService.cs` | Kontrakt `ListOperatorsAsync`, osobno od `IAccountService`, bo to jest czyste odczytanie, nie rejestracja |
| `backend/src/Ocwip.Api/Services/OperatorDirectoryService.cs` | Czyta te same konta, które `CompetitionService.FindContactsAsync` uznaje za poprawną osobę kontaktową: rola Operator i `IsActive`, posortowane po nazwisku i imieniu. Lista pokazująca konto, którego zapis by nie przyjął, byłaby gorsza niż brak listy |
| `backend/src/Ocwip.Api/Endpoints/PasswordResetEndpoints.cs` | `MapPasswordResetEndpoints` (T-12.4): `POST /forgot-password` (zawsze 200, 503 bez bazy, ten sam wzorzec `[FromServices]` nullable co `AccountEndpoints.cs`) i `POST /reset-password` (200 przy sukcesie, 400 `ProblemDetails` z jednym ogólnym komunikatem dla `PasswordResetOutcome.InvalidToken`, 400 `ValidationProblem` z `fieldErrors["newPassword"]` dla złego hasła, w tym samym kształcie co `/register`) |
| `backend/src/Ocwip.Api/Data/AppDbContext.cs` | `IdentityUserContext<User, Guid>`, nie `IdentityDbContext`, więc `AspNetRoles`/`AspNetUserRoles` nie istnieją: rola jest kolumną na koncie (`Role.cs`), nie tabelą Identity. `DbSet` dla `Entities`, `Competitions`, `FormDefinitions`, `Applications`, `Attachments` i `ApplicationStatusHistory` (T-33), konfiguracje wczytywane z assembly. Trzy tabele Identity (`IdentityUserClaim`, `IdentityUserLogin`, `IdentityUserToken`) przemianowane na `user_claims`/`user_logins`/`user_tokens` i przestawione z `Cascade` na `NoAction`. `ConfigureConventions` narzuca `UtcDateTimeOffsetConverter` na każdą właściwość `DateTimeOffset`. `SaveChanges` stempluje `IAuditedEntity`, bo domyślna wartość `now()` odpala się tylko przy INSERT |
| `backend/src/Ocwip.Api/Models/IAuditedEntity.cs` | Kontrakt `CreatedAt` i `UpdatedAt`, po którym `AppDbContext` znajduje encje do stemplowania |
| `backend/src/Ocwip.Api/Data/AppDbContextFactory.cs` | `IDesignTimeDbContextFactory` dla `dotnet ef`: `Create` i `BuildConfiguration` czytają `ConnectionStrings:Postgres` z tych samych źródeł co runtime, bez niego rzucają wyjątkiem zamiast zgadywać adres. `BuildConfiguration` jest `internal`, bo czyta z niego też komenda administracyjna |
| `backend/src/Ocwip.Api/Data/EmailNormalizer.cs` | `Normalize`: znormalizowany adres liczony **prawdziwym** `UpperInvariantLookupNormalizer` z Identity, nie jego odpowiednikiem. Dla dróg bez kontenera DI, czyli komendy administracyjnej i testów schematu; kod z DI ma brać `ILookupNormalizer`. To **nie** jest to samo co `upper(normalize(<adres>, NFC))` w SQL, którym liczy migracja i `seed.py`, i różnica jest przypięta testem |
| `backend/src/Ocwip.Api/Data/PostgresDbContextOptions.cs` | `UseOcwipPostgres`: Npgsql plus konwencja `snake_case`. Jedyne miejsce, w którym konfiguruje się model EF |
| `backend/src/Ocwip.Api/Data/Converters/UtcDateTimeOffsetConverter.cs` | Normalizuje każdy `DateTimeOffset` do UTC przed zapisem. Npgsql odrzuca timestamptz z niezerowym offsetem, więc to jest kontrakt, a nie komentarz |
| `backend/src/Ocwip.Api/Data/DatabaseStartup.cs` | `ApplyPendingMigrations`: migracje przy starcie pod flagą `Database:MigrateOnStartup`, pięć prób z narastającym opóźnieniem tylko dla błędów chwilowych |
| `backend/src/Ocwip.Api/Data/Migrations/20260819110449_InitialCreate.cs` | Migracja bazowa, pusta. `Down()` to no-op, bo `Up()` nic nie tworzy |
| `backend/src/Ocwip.Api/Data/Migrations/20260819110449_InitialCreate.Designer.cs` | Metadane EF dla `InitialCreate` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/AppDbContextModelSnapshot.cs` | Bieżący snapshot modelu EF (generowany) |
| `backend/src/Ocwip.Api/Endpoints/SessionEndpoints.cs` | `MapSessionEndpoints`: `POST /login` (200 z `LoginResponse`, 401 z jednym wspólnym komunikatem na złe dane, 403 gdy adres niepotwierdzony, 503 bez bazy), `POST /logout` (zawsze 200, bo wylogowanie jest idempotentne i 401 uczyłby front traktować sukces jak błąd) i `GET /me` (`RequireAuthorization`, 401 gdy wiersza już nie ma albo jest nieaktywny). Komunikaty jako stałe `internal`, żeby test cytował ten sam tekst, który leci do użytkownika. `sessions` jako nullowalny `[FromServices]` z tego samego powodu co w `AccountEndpoints.cs` |
| `backend/src/Ocwip.Api/Endpoints/CompetitionEndpoints.cs` | `MapCompetitionEndpoints`: sześć tras operatora (`POST /competitions`, `GET /competitions`, `GET|PUT|DELETE /competitions/{id}`, `POST /competitions/{id}/status`) z polityką roli per trasa, oraz dwie publiczne (`GET /public/competitions`, `GET /public/competitions/{id}`) z jawnym `AllowAnonymous`. Szkic dostaje **404**, nie 403. Mapowanie wyniku na kod w jednej metodzie `Failure`. Polskie nazwy stanów do komunikatów (D12) |
| `backend/src/Ocwip.Api/Endpoints/FormDefinitionEndpoints.cs` | `MapFormDefinitionEndpoints` (formularz wniosku, `FormPurpose.Application`; `Failure` i `Problems` wspólne z trasami kart oceny): trzy trasy operatora (`POST` i `GET /competitions/{id}/form-definitions` oraz `GET /competitions/{id}/form-definitions/{version}`). **Brakująca czwarta trasa jest projektem**: nic nie podmienia zapisanego dokumentu. Odmowa bramki kontraktu wychodzi jako 400 ze ścieżką JSON w kluczu, reszta przez wspólne `Failure`. Dwa różne 404 (nie ma konkursu kontra nie ma wersji), bo ten sam komunikat wysyłałby operatora, który pomylił konkurs, na poszukiwanie wersji |
| `backend/src/Ocwip.Api/Endpoints/EvaluationCardEndpoints.cs` | Wersje kart oceny (T-38): `POST`/`GET /competitions/{id}/evaluation-cards/{formal|merit}` i `GET .../{version}`, tylko operator, to samo wersjonowanie co formularz wniosku z przeznaczeniem w adresie |
| `backend/src/Ocwip.Api/Endpoints/EvaluationEndpoints.cs` | Oceny (T-38): `POST /applications/{id}/evaluations/formal` (operator), `.../merit` (recenzent przypisany do wniosku, polityka zasobu z T-37), `GET`/`PUT /evaluations/{id}` i `POST /evaluations/{id}/finish` za `EvaluationAccessHandler` |
| `backend/src/Ocwip.Api/Endpoints/ApplicationEndpoints.cs` | `MapApplicationEndpoints` (T-29): `POST /competitions/{id}/applications` (tylko wnioskodawca, pod jego własnym podmiotem), `GET`/`PUT`/`DELETE /applications/{id}`, `GET /applications/{id}/form-definition` (T-34). Te trasy nie niosą własnej polityki roli: wczytują zasób, pytają `IAuthorizationService` o politykę `resource.owner` z T-13.2, dopiero potem działają, w tej samej kolejności co `Authorization/PolicyProbeEndpoints.cs`, pierwszy prawdziwy endpoint produktowy idący tą drogą. `DELETE` nigdy nie usuwa wiersza, tylko ustawia `IsActive = false` (reguła 5) |
| `backend/src/Ocwip.Api/Endpoints/ApplicationSubmissionEndpoints.cs` | `MapApplicationSubmissionEndpoints` (T-33): `POST /applications/{id}/submit` niesie politykę roli `Applicant` NA TRASIE, oprócz zasobowej `resource.owner`, bo operator, choć widzi każdy wniosek, nie może złożyć cudzej oferty. `GET /applications/{id}/confirmation` (PDF potwierdzenia) niesie tylko `resource.owner`, jak każdy inny odczyt zasobu |
| `backend/src/Ocwip.Api/Endpoints/ApplicationAssignmentEndpoints.cs` | `MapApplicationAssignmentEndpoints` (T-37): `POST /applications/{id}/assignments` i `DELETE /applications/{id}/assignments/{reviewerId}`, obie za polityką roli `Operator` NA TRASIE, nie za `resource.owner`: operator zarządza przypisaniami na każdym wniosku, nie ma tu właściciela do sprawdzenia. Regułę widoczności po stronie recenzenta czyta `EntityScopedHandler.cs` z tej samej tabeli |
| `backend/src/Ocwip.Api/Endpoints/ApplicationListEndpoints.cs` | `MapApplicationListEndpoints` (T-35): `GET /competitions/{id}/applications`, `GET /competitions/{id}/applications/{applicationId}`, eksport `.../export/csv` i `.../export/pdf`. Wszystkie tylko dla operatora (polityka roli, nie zasobu: lista z definicji pokazuje cudze wnioski), plik nazwany numerem konkursu |
| `backend/src/Ocwip.Api/Endpoints/ApplicationOverviewEndpoints.cs` | `MapApplicationOverviewEndpoints` (T-34): `GET /applications`, tylko dla wnioskodawcy (polityka roli: zakres decyduje `EntityId` wywołującego, nie identyfikator z trasy, więc nie ma tu zasobu do wczytania przed autoryzacją) |
| `backend/src/Ocwip.Api/Endpoints/AttachmentEndpoints.cs` | `MapAttachmentEndpoints` (T-32): `POST /applications/{id}/attachments` (multipart, autoryzacja przez zasób WNIOSKU, bo załącznik jeszcze nie istnieje), `GET`/`PUT /attachments/{id}` (autoryzacja przez zasób ZAŁĄCZNIKA, prywatny `AuthorizeAsync`, ten sam wzorzec co `ApplicationEndpoints.cs`), `GET /applications/{id}/attachments` (T-34, lista, autoryzacja przez zasób WNIOSKU jak upload). Upload i lista dzielą jedną prywatną `AuthorizeAgainstApplicationAsync` zamiast każdy po swojemu ładować i pytać. Pobranie zawsze `Content-Disposition: attachment` z typem treści z wykrytego formatu, nigdy z deklaracji klienta |
| `backend/src/Ocwip.Api/Endpoints/HealthEndpoints.cs` | `MapHealthEndpoints`: `GET /health` (liveness, `Ok<HealthResponse>`) i `GET /health/db` (sonda PostgreSQL przez Npgsql, `Ok<DatabaseHealthResponse>` albo 503 z generycznym komunikatem, żeby nie ujawnić hosta ani poświadczeń). Ciała są nazwanymi rekordami z `Contracts/`, a nie typami anonimowymi, bo tylko nazwany typ da się opisać w dokumencie OpenAPI. 503 deklarowane przez `.ProducesProblem` (zgodność typu treści z tym, co naprawdę leci) |
| `backend/src/Ocwip.Api/appsettings.json` | Domyślne poziomy logowania, pusty `ConnectionStrings:Postgres`, `Database:MigrateOnStartup` fałsz, `Cors:Origins`. Sekcja `EmailVerification` (`FrontendBaseUrl`, `TokenLifetimeHours`, `ResendCooldownSeconds`/`Multiplier`/`MaxSeconds`/`ResetSeconds`) dla `EmailVerificationService.cs`. Wartości nadpisuje środowisko |
| `backend/src/Ocwip.Api/appsettings.Development.json` | Gadatliwsze logowanie ASP.NET Core lokalnie, `Database:MigrateOnStartup` prawda |
| `backend/tests/Ocwip.Api.Tests/Ocwip.Api.Tests.csproj` | xunit plus `Microsoft.AspNetCore.Mvc.Testing`, referencja do projektu API |
| `backend/tests/Ocwip.Api.Tests/OcwipWebApplicationFactory.cs` | `WebApplicationFactory` z wyłączonym `Database:MigrateOnStartup`. Każdy test startujący aplikację idzie tędy, żeby nie robić DDL na wspólnej bazie |
| `backend/tests/Ocwip.Api.Tests/CapturedLogs.cs` | `ILoggerProvider` zbierający wszystko, co aplikacja zalogowała w trakcie testu, razem z wyjątkami. Istnieje, bo reguła 4 z `AGENTS.md` zakazuje haseł w logach, a ciało odpowiedzi nic o logu nie mówi. Do ponownego użycia w T-12.2 i T-12.4 |
| `backend/tests/Ocwip.Api.Tests/RequiresDatabaseFactAttribute.cs` | `[RequiresDatabaseFact]` plus `ConnectionString` ze środowiska: fakt raportujący Skipped, a nie Passed, gdy nie ma bazy (xUnit 2 nie ma dynamicznego pomijania) |
| `backend/tests/Ocwip.Api.Tests/RequiresDatabaseTheoryAttribute.cs` | `[RequiresDatabaseTheory]`: to samo dla teorii. Nie da się tego dziedziczyć po wersji faktowej, bo xUnit rozróżnia `FactAttribute` i `TheoryAttribute`, a teoria z atrybutem faktowym traci swoje wiersze danych |
| `backend/tests/Ocwip.Api.Tests/HealthEndpointsTests.cs` | Cztery testy przez `OcwipWebApplicationFactory`: `/health` zwraca 200 także przy nieosiągalnej bazie, sonda bazy zwraca 503 bez connection stringa, sonda nigdy nie zwraca w ciele hasła ani użytkownika |
| `backend/tests/Ocwip.Api.Tests/Endpoints/RegistrationEndpointTests.cs` | `POST /register` po prawdziwym HTTP na prawdziwym PostgreSQL. Rdzeniem jest porównanie pełnego **odcisku odpowiedzi** (status, stabilne nagłówki, ciało) dla adresu zajętego i wolnego, plus ten sam odcisk dla adresu różniącego się wielkością liter, dla konta dezaktywowanego i dla duplikatu, który przeszedł obok walidatora prosto na 23505. Do tego adres wklejony z białym znakiem jako to samo konto, 503 na hoście bez bazy, polityka hasła warunek po warunku po polsku i dowód, że hasło nie trafia ani do ciała, ani do logu |
| `backend/tests/Ocwip.Api.Tests/Contracts/RegisterRequestValidatorTests.cs` | Reguła adresu, która w T-12.0 została jawnie przekazana na brzeg API: formy odrzucane (pusta, bez domeny, z nazwą wyświetlaną, dłuższa niż kolumna) i formy legalne, w tym `o'brien@example.org`, którą odrzucałby domyślny filtr Identity. Plus adres, który `MailAddress` przepuszcza, a `EmailAddressAttribute` nie (`"a@b"@example.org`), adresy i imiona ze znakiem niewidocznym, obcinanie białych znaków z pominięciem hasła, pole `null` z JSON-a, dowód, że polityka hasła NIE jest tu powtórzona, dowód, że `ToString` żądania nie zawiera hasła, i przypięte polskie komunikaty |
| `backend/tests/Ocwip.Api.Tests/Contracts/CompetitionIntakeMessageTests.cs` | Treść odmowy (D12): komunikat nazywa datę i godzinę zamknięcia w czasie polskim, a nie samą regułę. Godzina ścienna trzyma się po obu stronach zmiany czasu w październiku, nabór ciągły nie dostaje wymyślonej daty, żaden stan nie milczy |
| `backend/tests/Ocwip.Api.Tests/Services/LoginLandingPathTests.cs` | Tabela ról i katalog odrzuconych celów przekierowania: adres bezwzględny, protokołowo względny `//`, backslash, ścieżka względna, znak sterujący, pusty. Plus dwa testy, które łatwo przeoczyć: nieznana rola spada na panel wnioskodawcy, a odrzucona propozycja nie degraduje operatora do panelu wnioskodawcy. Plus `SafeOrNull` wprost: przycięta ścieżka, null dla odrzuconej i pustej, granica `MaxLength` |
| `backend/tests/Ocwip.Api.Tests/Services/RoleClaimsPrincipalFactoryTests.cs` | Kolumna `role` jako claim, dla każdej z trzech ról, i dokładnie jeden taki claim. Osobny test, bo brak claimu niczego nie wywraca: polityki z T-13.2 po prostu odmawiałyby wszystkiego, a objaw wyszedłby w tamtej karcie |
| `backend/tests/Ocwip.Api.Tests/Services/AccountServiceTests.cs` | To, co serwis robi bez Identity: anulowany token przerywa rejestrację, zanim cokolwiek dotknie `UserManager`. Reszta ścieżki jest sprawdzana po HTTP w `RegistrationEndpointTests`, bo serwis celowo nie potrafi zaraportować różnicy między utworzonym kontem a zajętym adresem |
| `backend/tests/Ocwip.Api.Tests/MigrationTests.cs` | Migracje na czystej bazie przez `UseOcwipPostgres`, na bazie z `ThrowawayDatabase`. Skip, gdy brak connection stringa |
| `backend/tests/Ocwip.Api.Tests/AttachmentMigrationTests.cs` | Co migracja T-32 (`AddAttachmentEntityIdAndFormat`) robi z załącznikami, które już są w bazie: `entity_id` bierze się z wniosku, `format` z typu MIME, a gdy typ nie jest nasz, z rozszerzenia nazwy pliku. Wiersz, którego formatu nie da się ustalić, zatrzymuje migrację na `P0001` bez połowicznie zaaplikowanego schematu. Wiersz zapisany przez obecny model, potem łańcuch cofnięty przez `IMigrator` przed migrację |
| `backend/tests/Ocwip.Api.Tests/IdentityMigrationTests.cs` | Co migracja T-12.0 robi z **danymi**, których `MigrationTests` nie widzi (migruje bazę pustą): adres potwierdzony przed Identity zostaje potwierdzony, wycofanie migracji oddaje potwierdzenie starej kolumnie, adres z akcentem zapisanym rozkładowo dostaje `normalized_email` liczone tak, jak liczy je normalizator Identity, a dwa adresy różniące się tylko wielkością liter albo tylko zapisem akcentu zatrzymują migrację na `P0001` z listą kolidujących adresów i bez połowicznie zaaplikowanego schematu. Migracja do wskazanego kroku przez `IMigrator` |
| `backend/tests/Ocwip.Api.Tests/Configuration/AuthenticationConfigurationTests.cs` | Ustawienia, które dają aplikację odpowiadającą 200 na logowanie i zachowującą się tak, jakby nikt się nie zalogował: czas życia <= 0 i `SameSite=None` bez `Secure`. Oba mają kończyć się odmową startu. Czwarty test pilnuje, że **pusta** wartość `Auth:SecureCookie` idzie za środowiskiem, bo to jest to, co przekazuje `docker-compose.yml`, i to jest różnica między lokalnym http a ciasteczkiem bez `Secure` na produkcji |
| `backend/tests/Ocwip.Api.Tests/Configuration/IdentityConfigurationTests.cs` | Opcje Identity czytane tak, jak czyta je aplikacja: polityka hasła, `RequireUniqueEmail`, wyłączony filtr znaków nazwy użytkownika, dowód NIEOBECNOŚCI progów blokady (należą do T-12.3), plus jeden test zachowania na prawdziwym `UserManager`: adres z apostrofem naprawdę się rejestruje. Dokłada też czas życia tokenu z `AddIdentityConfiguration` (domyślny i skonfigurowany), przeniesiony tu z dawnego `IdentityConfigurationTests.cs` w korzeniu projektu testów, żeby nie było dwóch klas o tej samej nazwie odpowiadających za jedną kartę |
| `backend/tests/Ocwip.Api.Tests/DatabaseConfigurationTests.cs` | Fabryka design-time rzuca przy braku connection stringa, `UseOcwipPostgres` daje Npgsql plus konwencję nazw |
| `backend/src/Ocwip.Api/Models/User.cs` | Model konta: `IdentityUser<Guid>` plus `IAuditedEntity`. Z bazy przychodzą `Id`, e-mail z kopią znormalizowaną, `EmailConfirmed`, hash hasła, `SecurityStamp` i kolumny blokady. Własne: `FirstName`, `LastName`, `Role` (domyślnie `Applicant`), `Pesel` (nullable, wrażliwe, do zaszyfrowania), `IsActive` (soft delete, sparowane w bazie z `DeactivatedAt`), znaczniki audytowe i relacja 1:1 z `Entity.cs`. Status weryfikacji adresu e-mail to wyłącznie `EmailConfirmed` z Identity (zob. `EmailVerificationService.cs`), dawna flaga `IsVerified` **usunięta** na jej rzecz; rejestracja świadomie nie pyta o PESEL, patrz `RegisterRequest.cs`. Bez `DataAnnotations`, mapowanie żyje w `Data/Configurations/UserConfiguration.cs` |
| `backend/src/Ocwip.Api/Models/Role.cs` | Enum jedzie po drucie **nazwą, nie numerem**: `[JsonConverter(typeof(JsonStringEnumConverter<Role>))]` na typie, nie opcja w `Program.cs`, bo opcja obowiązuje tylko w potoku HTTP. Enum trzech ról: `Applicant`, `Operator`, `Reviewer`. `Applicant` pierwszy celowo, więc CLR-owa wartość domyślna jest najmniej uprzywilejowana |
| `backend/tests/Ocwip.Api.Tests/Endpoints/EmailVerificationEndpointsTests.cs` | `/register`, `/verify-email` i `/resend-verification` na prawdziwym PostgreSQL, kolekcja `postgres`: rejestracja wysyła mail z działającym linkiem, link szanuje `FrontendBaseUrl` skonfigurowany, weryfikacja potwierdza konto i jest no-opem przy drugim kliknięciu tego samego linku, odrzuca token uszkodzony i nieznane konto, odsyłanie milczy identycznie dla adresu nieznanego, już potwierdzonego i w trakcie throttlingu, a bezpośrednio po rejestracji jest throttlowane od razu. `IEmailSender` podmieniony na `RecordingEmailSender`. Od T-12.8 także `returnUrl`: bezpieczny trafia do linku i nie psuje tokenu, adres bezwzględny, `//`, backslash i ścieżka względna są pomijane przy wciąż wysłanym mailu, ponowna wysyłka niesie go do nowego linku |
| `backend/tests/Ocwip.Api.Tests/Endpoints/SessionTestHost.cs` | Wspólne rusztowanie testów logowania: host wpięty w bazę jednorazową, zakładanie konta przez `UserManager` (potwierdzone, niepotwierdzone, nieaktywne, operator - roli nie da się nadać przez HTTP), klient **bez** obsługi ciasteczek i wyciąganie ciasteczka z `Set-Cookie`. Bez obsługi ciasteczek, bo połowa tych testów odtwarza ciasteczko, które przeglądarka już by wyrzuciła |
| `backend/tests/Ocwip.Api.Tests/Endpoints/SessionEndpointsTests.cs` | `/login`, `/logout` i `/me` po prawdziwym HTTP. Odcisk odpowiedzi (bez `traceId`, który różni się z definicji) dla złego hasła, nieznanego adresu, konta nieaktywnego i konta niepotwierdzonego z błędnym hasłem. Osobno: cel przekierowania per rola, `returnUrl` przyjęty i `returnUrl` na obcy adres odrzucony, komunikat o niepotwierdzonym adresie dopiero po poprawnym haśle, wylogowanie unieważniające **kopię** ciasteczka, konto wyłączone w trakcie sesji, 401 jako `problem+json` zamiast 302, hasło nieobecne w ciele i w logu, 503 bez bazy oraz routing, który na hoście bez bazy nadal stoi |
| `backend/tests/Ocwip.Api.Tests/Endpoints/FixedTimeProvider.cs` | Zegar, którym test przesuwa czas. Bez niego nie da się dojść do minuty zamknięcia (D7) |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionTestHost.cs` | Host z podmienionym `TimeProvider`, logowanie w wybranej roli i żądanie przechodzące całą walidację |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionAccessTests.cs` | Kto może dotknąć konkursu: 403 dla wnioskodawcy i recenzenta na **każdej** trasie operatora, 401 dla anonima, 200 na obu trasach publicznych bez konta |
| `backend/tests/Ocwip.Api.Tests/Endpoints/OperatorDirectoryEndpointTests.cs` | `GET /accounts/operators` (T-22): tylko aktywne konta z rolą Operator, posortowane po nazwisku i imieniu, konto wnioskodawcy i dezaktywowany operator wypadają z listy, 403 dla wnioskodawcy i recenzenta, 401 dla anonima |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionLifecycleEndpointTests.cs` | Droga operatora po HTTP: nowy konkurs zawsze roboczy, pełny przebieg z zegarem w środku, ruch spoza tabeli jako 409 z nazwą stanu, brak wcześniejszego otwarcia naboru, nabór ciągły, dezaktywacja (bez dozwolonych ruchów, z oddanym numerem), unikalny numer, dwa równoległe zapisy bez 500, granice kwoty. Po przesunięciu zegara test loguje się ponownie, bo handler ciasteczek czyta ten sam zegar |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionPublicViewTests.cs` | Widok gościa: szkic bez publicznego adresu (404), publikacja nadaje adres, konkurs nieaktywny znika tylko z widoku publicznego, archiwalny wypada z listy, ale zachowuje adres |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionTimeAndValidationTests.cs` | Czas i walidacja na brzegu: czas lokalny zapisany w UTC z uciętymi sekundami, okno zwijające się do jednej minuty, obie strony sparowania naboru ciągłego, puste pola nazwane po kolei, przycięcie spacji |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionParametersTests.cs` | Parametry kroków 1.2 do 1.6 po HTTP: pełny obieg zapisu i odczytu, domyślne kategorie kosztów, podmiana list przy edycji (nie doklejanie), widok gościa bez treści po złożeniu, sparowanie papieru, próg pięciu lat na dane osobowe (także przy naborze ciągłym), limity kwotowe i procentowe, adres regulaminu, indeksowane błędy załączników, osoba kontaktowa wyłącznie z konta pracownika |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CompetitionIntakeEndpointTests.cs` | Odcięcie terminu po HTTP: widok gościa przed startem, minutę przed zamknięciem i w minucie zamknięcia, nabór ciągły pięć lat później, ta sama odpowiedź dla operatora i gościa, konkurs dezaktywowany w otwartym oknie |
| `backend/tests/Ocwip.Api.Tests/Endpoints/SessionLifetimeTests.cs` | Ile sesja żyje i czym jest ciasteczko. Zegar podmieniany przez `TimeProvider` na opcjach schematu, bo suita, która śpi przez długość sesji, to suita, którą ktoś wyłączy: po przekroczeniu `Auth:SessionLifetimeHours` żądanie dostaje 401, a sesja używana co 90 minut przeżywa trzy godziny. Do tego atrybuty `Set-Cookie`: `HttpOnly`, `SameSite=Lax`, `Path=/` oraz `Secure`, gdy `Auth:SecureCookie` jest włączone |
| `backend/tests/Ocwip.Api.Tests/Endpoints/CorsCredentialsTests.cs` | Preflight CORS dla `/register`: dozwolony origin dostaje `Access-Control-Allow-Credentials: true`, bo bez tego przeglądarka nie wyśle ciasteczka sesyjnego, a origin spoza `Cors:Origins` nie dostaje żadnego nagłówka CORS. Druga asercja jest tą, która pilnuje, żeby konfiguracja nie zsunęła się do `AllowAnyOrigin`. Połowa kryterium sesji z T-17; 401 bez ciasteczka wymaga schematu uwierzytelniania i należy do T-12.3 |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ErrorFormatTests.cs` | Jeden format błędu w całym API: `/register` odpowiada `application/problem+json` przy 503 (host bez bazy) i przy 400 (odrzucona walidacja, connection string prowadzący donikąd, więc bez prawdziwej bazy), a dokument OpenAPI deklaruje ten sam typ treści dla `/register` 400 i 503, `/verify-email` 400 oraz `/health/db` 503. Ta druga asercja pilnuje regresji, w której `.Produces<ProblemDetails>` deklaruje `application/json` |
| `backend/tests/Ocwip.Api.Tests/Endpoints/PasswordResetEndpointTests.cs` | `/forgot-password` i `/reset-password` po prawdziwym HTTP na prawdziwym PostgreSQL (T-12.4): pełna ścieżka (link z maila zmienia hasło, stare hasło już nie działa), reset unieważniający aktywną sesję (ciasteczko z logowania przestaje działać po resetcie), token użyty dwa razy, token wygasły (`PasswordReset:TokenLifetimeHours` ustawione na 0, żeby token był przeterminowany bez czekania na zegar), token uszkodzony, nieznany `userId`, nieznany adres w `/forgot-password` (200, brak maila), polityka hasła po polsku, hasło nieobecne w ciele i w logu |
| `backend/tests/Ocwip.Api.Tests/Authorization/PolicyProbeEndpoints.cs` | Trasy istniejące wyłącznie w testach (T-13.2, T-13.3), wpinane w **prawdziwy** potok aplikacji przez `IStartupFilter`: trasa tylko dla operatora, trasa bez żadnej reguły (ta, którą ktoś zapomniał), trasa zasobowa oraz `ApplicationById` z T-13.3. Ta czwarta jest jedyną w kształcie prawdziwego endpointu produktowego: bierze identyfikator WNIOSKU, czyta wiersz z bazy i dopiero wczytany zasób oddaje do autoryzacji, bo trasa ufająca identyfikatorowi z adresu przechodzi wszystkie testy T-13.2 i wycieka. `AppDbContext` jako nullowalny `[FromServices]` z tego samego powodu co w `AccountEndpoints.cs`. Wiersz nieistniejący to 404, wiersz cudzy to 403 (uzasadnienie w `architektura.md`). Ciało sukcesu niesie odpowiedzi wniosku celowo, żeby test mógł twierdzić o braku cudzych danych, nie tylko o kodzie statusu |
| `backend/tests/Ocwip.Api.Tests/Authorization/AuthorizationLayerTests.cs` | Co widzi WOŁAJĄCY (T-13.2): trasa bez reguły odmówiona, 401 dla niezalogowanego i 403 dla zalogowanego bez roli, odmowa jako `problem+json` po polsku zamiast 500 i pustej strony, operator widzi cudzy zasób, wnioskodawca tylko swój, konto bez podmiotu nic, recenzent odmówiony na zasobie, który nie jest wnioskiem (przypadek pozytywny, prawdziwy wniosek plus przypisanie, jest w `ReviewerAssignmentTests.cs`, T-37) |
| `backend/tests/Ocwip.Api.Tests/Authorization/AuthorizationConfigurationTests.cs` | Kształt samego zbioru reguł: `FallbackPolicy` wymaga zalogowania, każda wartość enuma `Role` ma politykę (przypięcie R-02), polityka zasobowa niesie swoje wymaganie, oraz strażnik regresji dla domyślnej odmowy - każda dzisiejsza trasa publiczna i dokument OpenAPI nadal osiągalne bez sesji |
| `backend/tests/Ocwip.Api.Tests/Authorization/PermissionScenario.cs` | Zestaw danych dla testów negatywnych (T-13.3): jeden konkurs, jedna wersja formularza, **dwa** podmioty i po jednym wniosku (złożony z numerem pod pierwszym, roboczy pod drugim), plus zakładanie i logowanie konta w każdej roli. Kształt jest świadomie taki sam jak w `scripts/seed.py`, ale odtworzony w kodzie, bo zasiane konta mają w `password_hash` placeholder i **nie potrafią się zalogować**, a ta suita przechodzi prawdziwe `POST /login` na bazie jednorazowej dzielonej przez kolekcję `postgres`, której seed nie dotyka. Markery w odpowiedziach obu wniosków istnieją, żeby wyciek dał się sprawdzić jako NIEOBECNOŚĆ cudzych danych w ciele, a nie jako sam status |
| `backend/tests/Ocwip.Api.Tests/Authorization/PermissionDenialTests.cs` | Testy negatywne uprawnień (T-13.3) po prawdziwym HTTP: wnioskodawca A nie dostaje wniosku wnioskodawcy B (i odwrotnie, bo reguła zależna od kolejności insertów przeszłaby jednostronny test), podmiana identyfikatora w adresie przechodząca wszystkie wnioski odczytane z bazy (więc także wiersze zostawione przez inne klasy w kolekcji) plus jeden nieistniejący, wnioskodawca na trasie operatora, recenzent bez przypisania odmówiony na obu wnioskach scenariusza (T-37: przypadek pozytywny jest w `ReviewerAssignmentTests.cs`), niezalogowany na `/me` i na wszystkich trzech sondach. Każda odmowa jest sparowana z wołającym, który na tej samej trasie **przechodzi**, bo suita odmawiająca wszystkiego wygląda identycznie jak suita z zepsutym endpointem |
| `backend/tests/Ocwip.Api.Tests/Authorization/PermissionSuiteCiGuardTests.cs` | Ostatnie kryterium T-13.3, T-36 i T-37, czyli "testy blokują merge", przypięte testem zamiast pamięci recenzenta: każda metoda w `PermissionDenialTests`, `ApplicantDataIsolationTests` i `ReviewerAssignmentTests` (`GuardedSuites`) musi iść przez `[RequiresDatabaseFact]` (zwykły `[Fact]` byłby zielony bez bazy, czyli bez potoku, który rzekomo dowodzi), a w CI (`CI` w środowisku) connection string musi być ustawiony, inaczej cała suita raportuje Skipped i nie blokuje niczego |
| `backend/tests/Ocwip.Api.Tests/Authorization/ApplicantDataIsolationTests.cs` | Testy izolacji danych wnioskodawcy (T-36), rozszerzenie T-13.3 na prawdziwą, kompletną ścieżkę: dwóch wnioskodawców w DWÓCH różnych konkursach (celowo, to jest "konkurs, w którym podmiot nie startował"), każdy z realną wersją roboczą, zapisanymi odpowiedziami i przesłanym załącznikiem. Podmiana identyfikatora wniosku, cudzej definicji formularza, cudzego załącznika (pobranie i lista) i cudzego złożenia, każda strona sparowana z właścicielem, który nadal przechodzi, i ze stanem w bazie sprawdzonym po odmowie (nic się nie zmieniło). Dokłada jeden test, którego nie było: `StoragePath` załącznika (goły GUID na dysku) nie działa jako adres pobrania z pominięciem `/attachments/{id}`, ani przy adresie serwera wprost, ani przy podstawieniu go pod trasę pobrania |
| `backend/tests/Ocwip.Api.Tests/Authorization/ReviewerAssignmentTests.cs` | T-37 na prawdziwej ścieżce produktowej, nie na sondzie: recenzent bez przypisania dostaje 403, po `POST /applications/{id}/assignments` dostaje 200, przypisanie w jednym konkursie nie odblokowuje wniosku z drugiego (dwie różne konkurencje w scenerii, jak w `ApplicantDataIsolationTests.cs`), relacja wiele do wielu (dwóch recenzentów na jednym wniosku, jeden recenzent na dwóch wnioskach, niezależnie), cofnięcie przez `DELETE .../assignments/{reviewerId}` dezaktywuje wiersz zamiast go kasować i od razu odbiera dostęp, tylko operator może przypisywać i cofać (wnioskodawca, recenzent i gość odmówieni), przypisanie nieistniejącego konta albo konta bez roli `Reviewer` odmówione jako 404 |
| `backend/tests/Ocwip.Api.Tests/Endpoints/LoginLockoutTests.cs` | Konto na prawdziwym HTTP (T-12.5): pięć złych haseł blokuje konto z czytelnym komunikatem 429, poprawne hasło w trakcie blokady wciąż odrzucone (bo `CheckPasswordSignInAsync` sprawdza blokadę przed hasłem), nieudane logowania w logu bez hasła |
| `backend/tests/Ocwip.Api.Tests/Endpoints/RateLimitingTests.cs` | Limit po adresie IP na prawdziwym HTTP (T-12.5): `/login`, `/register`, `/forgot-password`, `/resend-verification` odrzucają po przekroczeniu skonfigurowanego `RateLimiting:PermitLimit`, `Retry-After` w odpowiedzi, budżet współdzielony między wszystkimi czterema endpointami tej samej polityki |
| `backend/tests/Ocwip.Api.Tests/Endpoints/AuthenticationJourneyTests.cs` | Karta domykająca blok uwierzytelniania (T-12.6): jeden scenariusz na prawdziwym HTTP i prawdziwym PostgreSQL, rejestracja, weryfikacja maila, logowanie, wylogowanie (to samo ciasteczko przestaje działać), reset hasła, logowanie nowym hasłem. Sprawdza wyłącznie połączenie elementów pokrytych osobno w T-12.1 do T-12.5, bez powtarzania ich przypadków negatywnych, więc chodzi w kilka sekund |
| `backend/tests/Ocwip.Api.Tests/Endpoints/RecordingEmailSender.cs` | `IEmailSender` do testów: zbiera wysłane wiadomości w `ConcurrentQueue`, żeby test mógł sprawdzić, co poszłoby do skrzynki, bez prawdziwego transportu |
| `backend/tests/Ocwip.Api.Tests/ResendBackoffCalculatorTests.cs` | Wzrost i sufit `ResendBackoffCalculator.CooldownFor`: pierwsza próba to baza, każda kolejna mnoży przez współczynnik, sufit trzyma się dla dowolnie wielu prób, próba poniżej 1 rzuca |
| `backend/src/Ocwip.Api/Models/Entity.cs` | Model podmiotu: typ, nazwa, dane kontaktowe, nullable `Nip` i `Address` (wrażliwe, wymagalność zależna od typu pilnowana na brzegu API), soft delete, znaczniki audytowe, kolekcja wniosków |
| `backend/src/Ocwip.Api/Models/EntityType.cs` | Enum zawierający trzy typy podmiotów, na drucie jako tekst (od T-35, lista filtruje po nim) |
| `backend/src/Ocwip.Api/Models/Competition.cs` | Model konkursu: numer (unikalny), kwota dotacji, status, `IsContinuousIntake` (wyłącza `EndDate`, stąd jest ono nullowalne), `PublishedAt`, `FormalCardDefinitionId` i `MeritCardDefinitionId` (karty oceny w mocy, T-38), `FormDefinitionId` (wersja formularza w mocy), znaczniki czasu i flaga `IsActive` (bez twardego kasowania). Settery `StartDate` i `EndDate` ucinają do pełnej minuty w UTC, celowo nie konwerterem, patrz komentarz w pliku. Relacja jeden do wielu z `FormDefinition.cs` |
| `backend/src/Ocwip.Api/Models/CompetitionStatus.cs` | Enum siedmiu stanów konkursu z raportu (`R-17`): `Draft`, `Published`, `OpenForApplications`, `Closed`, `UnderReview`, `Resolved`, `Archived`. W bazie i po drucie zapisywany jako tekst, nie jako ordynał |
| `backend/src/Ocwip.Api/Models/CompetitionStatusTransition.cs` | Jedna dozwolona para stanów plus `TransitionTrigger`: `Operator` albo `Schedule` |
| `backend/src/Ocwip.Api/Models/CompetitionStatusTransitions.cs` | **Jedyna** tabela dozwolonych przejść między stanami konkursu (`R-17`). `AllowsOperator` celowo odrzuca pary terminowe, `OperatorTargets` oddaje je panelowi, `ScheduledTarget` mówi, dokąd przesuwa zegar. Dołożenie stanu to dopisanie wiersza |
| `backend/src/Ocwip.Api/Models/CompetitionLifecycle.cs` | Stan **efektywny**: zapisany status przesunięty o każde przejście terminowe, którego moment już minął (pętla, nie jeden krok). Zamiast zadania w tle, bo kolumna przestawiana cyklicznie kłamie dokładnie w minucie zamknięcia. Tu też `IsPubliclyVisible` (szkic nie ma publicznego adresu) |
| `backend/src/Ocwip.Api/Models/CompetitionIntake.cs` | **Jedyna** odpowiedź na pytanie "czy ten konkurs przyjmuje jeszcze wnioski" (T-21, D7): `IntakeState` (`Open`, `NotYetOpen`, `Closed`, `Unavailable`, tekstem po drucie), `CompetitionIntakeState` z momentem otwarcia i zamknięcia oraz `CompetitionIntake.For`. Zbudowane **na** `CompetitionLifecycle`, nie obok: ucinanie do pełnej minuty i nabór ciągły są już tam. Konkurs nieaktywny sprawdzany przed zegarem. Składanie, autozapis i upload (T-29, T-32, T-33) wołają to, zamiast porównywać daty u siebie |
| `backend/src/Ocwip.Api/Models/CompetitionAttachment.cs` | Wymagany załącznik jako ustawienie konkursu (krok 1.5), nie plik wnioskodawcy (to `Attachment.cs`). Wzoru pliku świadomie brak: przechowywanie plików to `T-32` |
| `backend/src/Ocwip.Api/Models/CompetitionContact.cs` | Osoba kontaktowa jako wskazanie konta pracownika, nie kopia nazwiska i adresu, żeby strona konkursu nie pokazywała kontaktu do kogoś, kto odszedł |
| `backend/src/Ocwip.Api/Models/CompetitionCostCategory.cs` | Kategoria kosztów dopuszczona w tym konkursie. Obecność wiersza jest ustawieniem, brak wiersza wyłącza kategorię razem z jej sekcją opisową |
| `backend/src/Ocwip.Api/Models/CostCategory.cs` | Trzy kategorie kosztów z wzoru na 2026 (`R-12`), jako ustawienie konkursu, nie stała systemu |
| `backend/src/Ocwip.Api/Models/AttachmentRequirement.cs` | Trzy warianty wymagalności załącznika z raportu, nie sześć jak w obecnym narzędziu |
| `backend/src/Ocwip.Api/Models/AllowedFileFormat.cs` | Zamknięty zbiór formatów, w jakich wolno oddać załącznik. Zamknięty, bo czyta go kontrola uploadu z `T-32` |
| `backend/src/Ocwip.Api/Models/PercentageBasis.cs` | Podstawa liczenia procentów z kroku 1.4: kwota dotacji albo całkowita wartość projektu. Ustawienie konkursu, domyślnie kwota dotacji |
| `backend/src/Ocwip.Api/Models/FormDefinition.cs` | Model definicji formularza: struktura jako `JsonElement` w kolumnie jsonb plus numer wersji i przeznaczenie (T-38); wersja unikalna w obrębie pary konkurs plus przeznaczenie |
| `backend/src/Ocwip.Api/Models/Forms/FormFieldType.cs` | Piętnaście rodzajów pól z [`../runbook/pola.md`](../runbook/pola.md) plus tabela nazw po drucie i pytania o rodzaj (tekstowy, liczbowy, wybór, tabela, dozwolony w kolumnie). Wartość zerowa to `Unknown`, żeby pole bez rodzaju nie stało się po cichu tekstem |
| `backend/src/Ocwip.Api/Models/Forms/FormDocument.cs` | Rekordy kontraktu: dokument, sekcja, pole, opcja, tabela, warunek widoczności, obliczenie, limit (z `PercentFrom`, procentem z ustawienia konkursu, T-31) i reguły pliku. Korzeń jest obiektem z `schemaVersion`. `AllFields()` daje płaską listę kluczy razem z kolumnami tabel jako `tabela.kolumna` |
| `backend/src/Ocwip.Api/Models/Forms/FormFieldRole.cs` | Rola pola poza formularzem (T-35): tytuł projektu, całkowity koszt, wnioskowana kwota; od T-38 także kryterium formalne (jedyna rola powtarzalna), suma merytoryczna, suma strategiczna i kwota rekomendowana. Odczyt z `role`, odmowa nieznanej roli, roli na kolumnie tabeli, na nie pasującym rodzaju pola (iloraz to procent) i powtórzonej roli |
| `backend/src/Ocwip.Api/Models/Forms/FormPurpose.cs` | Przeznaczenie definicji (T-38, D16): wniosek, karta oceny formalnej, karta merytoryczna, jako tekst. `FormPurposeRules`: `appliesTo`, punkty i role oceny tylko na karcie, role wniosku tylko na wniosku, karta formalna bez kryterium i merytoryczna bez sumy odrzucone |
| `backend/src/Ocwip.Api/Models/Forms/FormEvaluationParts.cs` | Odczyt `appliesTo` (rodzaje wnioskodawcy w pisowni `EntityType` z API, z rozróżnianiem wielkości liter, niepusta lista bez powtórzeń, nigdy na kolumnie) i `points` (tylko tak lub nie poza tabelą, dodatnie) (T-38) |
| `backend/src/Ocwip.Api/Models/Forms/FormSchemaValidator.cs` | Bramka kontraktu: `JsonElement` wchodzi, wychodzi dokument albo komplet powodów odmowy. Jedyne publiczne wejście do walidacji schematu, z przeznaczeniem dokumentu (T-38, domyślnie wniosek) |
| `backend/src/Ocwip.Api/Models/Forms/FormSchemaValidationResult.cs` | Wynik walidacji plus `FormSchemaError`: ścieżka JSON do miejsca w dokumencie i polski komunikat wskazujący pole |
| `backend/src/Ocwip.Api/Models/Forms/AnswerValidator.cs` | Walidacja odpowiedzi wnioskodawcy (T-30), jedyne wejście: dokument, odpowiedzi, ustawienia konkursu i poziom surowości. Szkic odrzuca tylko to, czego renderer nie mógłby wysłać (klucz spoza formularza albo podwójny, zły kształt, nadmiar wierszy), złożenie dokłada wymagalność widocznych pól, zakresy, liczbę wierszy i limity. Jeden komunikat na klucz, w kolejności renderera. `MaxTableRows` to sufit tabeli zmiennej, podnoszony przez wyższe `maxRows` z definicji. Limit na sumie kolumny tabeli nazywa tabelę i dodaje komunikat na komórce pozycji, od której suma przekracza granicę (T-31) |
| `backend/src/Ocwip.Api/Models/Forms/AnswerValidationResult.cs` | `AnswerStrictness` (szkic, złożenie), `AnswerError` z kluczem renderera (`pole` albo `tabela[wiersz].kolumna`, jak `cellKey` we froncie) i wynik z `ToProblemErrors` dla `ValidationProblem` |
| `backend/src/Ocwip.Api/Models/Forms/AnswerShape.cs` | Kształt jednej odpowiedzi według rodzaju pola, na obu poziomach: tekst z `maxLength`, liczba, data i data z godziną w zapisie inputów, tak lub nie, opcja z listy, załącznik jako nazwa i rozmiar do czasu T-32, odmowa odpowiedzi w polu wyliczanym (D11) |
| `backend/src/Ocwip.Api/Models/Forms/AnswerValues.cs` | Odczyt odpowiedzi tak jak renderer: co jest puste, jaka liczba stoi za odpowiedzią, czy odpowiedź spełnia warunek (`"true"`/`"false"` dla tak lub nie, dowolna zaznaczona opcja dla wielokrotnego wyboru) |
| `backend/src/Ocwip.Api/Models/Forms/AnswerCalculator.cs` | Widoczność sekcji, pól i kolumn, `IsApplicable` dla pola zadanego tylko części wnioskodawców (T-38), punkty za "tak" w sumie (warunek kolumny czyta najpierw sąsiednią komórkę) oraz wartości pól wyliczanych na `decimal` z pełną precyzją (D13), z tymi samymi regułami co `frontend/lib/forms/evaluate.ts`. `Saturating`: arytmetyka nasycająca się zamiast wyjątku przepełnienia, bo liczby przychodzą z żądania |
| `backend/src/Ocwip.Api/Models/Forms/ApplicationRoleValues.cs` | Tytuł, koszt i dotacja jednego wniosku odczytane przez role pól jego wersji formularza, tym samym `AnswerCalculator` co walidacja. Pole ukryte warunkiem i puste pole kwoty dają null, nie zero |
| `backend/src/Ocwip.Api/Models/Forms/EvaluationScores.cs` | Wynik jednej karty odczytany przez role (T-38): formalna pozytywna, gdy każde kryterium zadane temu wnioskodawcy spełnione, negatywna przy pierwszym niespełnionym, otwarta przy braku odpowiedzi; sumy merytoryczna i strategiczna; kwota rekomendowana, pusta to brak rekomendacji, nie zero |
| `backend/src/Ocwip.Api/Models/Forms/AnswerLimits.cs` | Limity D12 odwrócone: `Find` zwraca `LimitBreach` z kwotą przekroczenia i granicą wyliczoną dla tego wniosku, procent bierze z liczby albo z ustawienia konkursu (`PercentFrom`, T-31). `BasesFor` mapuje ustawienia konkursu na nazwy `competition.*` z definicji; nieustawione ustawienie znaczy "limitu nie sprawdzamy" |
| `backend/src/Ocwip.Api/Models/Forms/PolishNumbers.cs` | Kwoty i procenty w polskim zapisie, tak jak `formatAmount` i `formatPercent` we froncie ("9000,00 zł", "12 500,50 zł", "10%"), pisane ręcznie, bo obraz bez ICU po cichu formatowałby po angielsku |
| `backend/src/Ocwip.Api/Models/Forms/FormJsonReader.cs` | Czytanie prymitywów definicji ze zbieraniem błędów zamiast rzucania wyjątkiem, plus postać klucza i wartości porównywalne w warunku |
| `backend/src/Ocwip.Api/Models/Forms/FormDocumentParser.cs` | Szkielet definicji: korzeń, wersja kontraktu, sekcje, pola, unikalność kluczy w całym dokumencie, sufity liczby sekcji i pól |
| `backend/src/Ocwip.Api/Models/Forms/FormFieldParser.cs` | Jedno pole: klucz, rodzaj, etykieta, wymagalność, flaga wydruku (D14), limity znaków i widełki wartości, warunek widoczności (wspólny z sekcją) |
| `backend/src/Ocwip.Api/Models/Forms/FormFieldParts.cs` | Części, które ma tylko część rodzajów: opcje wyboru, tabela z kolumnami i wierszami, obliczenie (suma z co najmniej jednym składnikiem, T-31), limity (procent albo `percentFrom`, nigdy oba), reguły pliku. Każda odrzucana na rodzaju, który jej nie używa |
| `backend/src/Ocwip.Api/Models/Forms/FormSchemaReferences.cs` | Sprawdzenia wymagające całego dokumentu: na co wskazuje warunek (i czy stoi wyżej), co czyta obliczenie, względem czego mierzy limit i z jakiego progu konkursu bierze procent, oraz cykl obliczeń. Suma poza tabelą może dodawać pola poza tabelą, kolumna nie (T-31) |
| `backend/src/Ocwip.Api/Data/Configurations/CompetitionConfiguration.cs` | Konfiguracja EF Core konkursu: limity długości, `gen_random_uuid()`, `now()` dla znaczników czasu, sześć check constraints (kolejność dat, dodatnia kwota, pełna minuta na `start_date` i `end_date`, sparowanie `is_active` z `deactivated_at`, sparowanie `end_date` z `is_continuous_intake`) w jednym wywołaniu `ToTable`, indeks na `(status, end_date)`, unikalny indeks na `number` **filtrowany po `is_active`** (konkurs dezaktywowany oddaje numer), złożony FK `(id, form_definition_id)` na klucz alternatywny `form_definitions`, FK bez kaskady |
| `backend/src/Ocwip.Api/Data/Configurations/CompetitionAttachmentConfiguration.cs` | Konfiguracja EF Core wymaganego załącznika: wymagalność i formaty jako tekst (`text[]` z komparatorem, nie tabela, bo zbiór jest czytany w całości i nigdy nie łączony), dwa check constraints, indeks na `(competition_id, position)`, kaskada do konkursu z uzasadnieniem |
| `backend/src/Ocwip.Api/Data/Configurations/CompetitionContactConfiguration.cs` | Konfiguracja EF Core osoby kontaktowej: unikalne `(competition_id, user_id)`, kaskada do konkursu, ale `NoAction` do konta, bo konto jest czyimś śladem, a wiersz ustawieniem |
| `backend/src/Ocwip.Api/Data/Configurations/CompetitionCostCategoryConfiguration.cs` | Konfiguracja EF Core kategorii kosztów: obecność wiersza jest ustawieniem (`R-12`), unikalne `(competition_id, category)`, kategoria jako tekst |
| `backend/src/Ocwip.Api/Data/Configurations/FormDefinitionConfiguration.cs` | Konfiguracja EF Core definicji formularza: kolumna jsonb, przeznaczenie jako tekst (T-38), cztery check constraints (znane przeznaczenie, sparowanie soft delete, `version_number > 0`, korzeń JSON-a jako obiekt albo tablica), unikalny indeks na `(competition_id, version_number)` |
| `backend/src/Ocwip.Api/Data/Migrations/20260827130815_AddDataModels.cs` | Tabele `competitions` i `form_definitions`, pięć check constraints na `competitions` i trzy na `form_definitions`, indeks na `(status, end_date)` dla listy publicznej, unikalny indeks na wersję formularza |
| `backend/src/Ocwip.Api/Data/Migrations/20260827130815_AddDataModels.Designer.cs` | Metadane EF dla `AddDataModels` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260918090434_AddCompetitionLifecycleFields.cs` | Kolumny cyklu życia konkursu: `number` (dodawana w trzech krokach, żeby unikalny indeks nie wszedł na wspólną wartość domyślną; indeks filtrowany po `is_active`), `published_at`, `is_continuous_intake`, `form_definition_id`, `end_date` staje się nullowalne, szósty check constraint i złożony FK. `Down()` odmawia z komunikatem, gdy istnieje konkurs z naborem ciągłym, bo nie ma czego wpisać w `end_date` |
| `backend/src/Ocwip.Api/Data/Migrations/20260918090434_AddCompetitionLifecycleFields.Designer.cs` | Metadane EF dla `AddCompetitionLifecycleFields` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260918094645_AddCompetitionWizardParameters.cs` | Parametry kroków 1.2 do 1.6: kolumny na `competitions` (rezultaty, regulamin, dwie treści po złożeniu, papier, ramy projektu, kwoty, procenty, podstawa procentu, próg przychodu, data przetwarzania danych, limity uploadu), osiem nowych check constraints oraz trzy tabele dzieci. Na koniec dosypuje trzy domyślne kategorie kosztów istniejącym konkursom, bo brak wierszy czyta się jako budżet bez kategorii |
| `backend/src/Ocwip.Api/Data/Migrations/20260918094645_AddCompetitionWizardParameters.Designer.cs` | Metadane EF dla `AddCompetitionWizardParameters` (generowane) |
| `backend/src/Ocwip.Api/Models/Application.cs` | Model wniosku: `Answers` jako `JsonElement` w jsonb, status, `SubmittedAt` i `Number` (oba nadawane przy złożeniu), soft delete, wskazania na konkurs, podmiot i wersję definicji formularza, kolekcja `StatusHistory` (T-33). Komentarz przy `CompetitionId` tłumaczy, dlaczego trzymamy je obok `FormDefinitionId` |
| `backend/src/Ocwip.Api/Models/ApplicationStatus.cs` | Enum dwóch statusów wniosku: `Draft`, `Submitted`. W bazie zapisywany jako tekst |
| `backend/src/Ocwip.Api/Models/ApplicationStatusHistory.cs` | Jeden wpis historii statusów wniosku (T-33): `FromStatus`, `ToStatus`, `ChangedAt`, `ChangedByUserId`. Dopisywany, nigdy aktualizowany; `FromStatus`/`ToStatus` nie są zawężone do pary Draft/Submitted, żeby przyszłe przejście z R-03 miało gdzie wylądować bez zmiany schematu |
| `backend/src/Ocwip.Api/Models/ApplicationAssignment.cs` | Przypisanie jednego recenzenta do jednego wniosku (T-37), wiele do wielu, nie jeden do jednego: pytania otwarte karty (ilu recenzentów, B-02) są nierozstrzygnięte, a zawężenie relacji później jest tanie, rozszerzenie drogie. `ReviewerId` wskazuje konto, którego rolę pilnuje serwis, nie schemat. Soft delete: cofnięcie ustawia `IsActive = false` zamiast kasować wiersz, jeden wiersz na parę (unikalny indeks), więc ponowne przypisanie reaktywuje ten sam wiersz |
| `backend/src/Ocwip.Api/Models/Evaluation.cs` | Jedna karta oceny wypełniona przez jedną osobę dla jednego wniosku (T-38): etap formalny albo merytoryczny, wersja karty zamrożona, autor jako konto albo nazwa (dokładnie jedno, decyzja 14 raportu), osoba wprowadzająca osobno, odpowiedzi JSONB, szkic albo zakończona. Bez zapisanego wyniku |
| `backend/src/Ocwip.Api/Models/Attachment.cs` | Model załącznika: nazwa pliku, typ MIME zadeklarowany (metadana, nieużywana do decyzji), format zweryfikowany z sygnatury bajtów, rozmiar, ścieżka w storage, powiązanie z wnioskiem, `EntityId` (kopia z wniosku, implementuje `IEntityScoped` wprost, bez joina), soft delete (T-32) |
| `backend/src/Ocwip.Api/Models/ApplicationChecksum.cs` | Suma kontrolna wersji roboczej (D15, R-13), licząca się na nowo z identyfikatora, ostatniego zapisu i odpowiedzi, nigdy niezapisywana. Klucze obiektu sortowane przed haszowaniem, bo `jsonb` w PostgreSQL nie trzyma ich kolejności ani białych znaków, więc suma musi zgadzać się sama ze sobą przed pierwszym zapisem i po każdym kolejnym |
| `backend/src/Ocwip.Api/Data/Configurations/UserConfiguration.cs` | Konfiguracja EF Core konta: `ToTable("users")` zamiast `AspNetUsers`, unikalny indeks na `normalized_email` (nazwa użytkownika celowo **bez** unikalności), `Ignore` na `phone_number`, `phone_number_confirmed` i `two_factor_enabled`, defaulty w bazie na kolumnach Identity `NOT NULL` (oba stampy wymagane, z `gen_random_uuid()::text`, `AccessFailedCount` z `DEFAULT 0` - zob. `20260907111921_RestoreAccessFailedCountDefault.cs`), `IsActive` (soft delete, wymagane), rola jako tekst z wartością domyślną `Applicant`, dwa check constraints (sparowanie soft delete, `ck_users_role_is_known` ograniczający rolę do trzech nazw), komentarze przy `PasswordHash` i `Pesel`, relacja 1:1 z podmiotem bez kaskady |
| `backend/src/Ocwip.Api/Data/Configurations/EntityConfiguration.cs` | Konfiguracja EF Core podmiotu: typ jako tekst (30 znaków, bo `PatronInformalGroup` ma 19), komentarze przy `Nip` i `Address`, sparowanie soft delete |
| `backend/src/Ocwip.Api/Data/Configurations/ApplicationConfiguration.cs` | Konfiguracja EF Core wniosku: kolumna jsonb, status jako tekst, cztery check constraints (sparowanie statusu z `submitted_at` i z `number`, korzeń JSON-a jako dokument, sparowanie soft delete), unikalny indeks na `(competition_id, number)`, indeks na `(competition_id, status)`, złożony FK `(competition_id, form_definition_id)`. Świadomy brak unikalności na `(entity_id, competition_id)` |
| `backend/src/Ocwip.Api/Data/Configurations/ApplicationStatusHistoryConfiguration.cs` | Konfiguracja EF Core historii statusów (T-33): `FromStatus`/`ToStatus` jako tekst, check constraint `from_status <> to_status`, indeks na `(application_id, changed_at)`, dwa FK (`Application`, `ChangedByUser`) oba `NoAction` |
| `backend/src/Ocwip.Api/Data/Configurations/ApplicationAssignmentConfiguration.cs` | Konfiguracja EF Core przypisania recenzenta (T-37): sparowanie soft delete check constraintem, unikalny indeks na `(application_id, reviewer_id)`, indeks `(reviewer_id, is_active)` pod odczyt `EntityScopedHandler.cs`, dwa FK (`Application`, `Reviewer`) oba `NoAction` |
| `backend/src/Ocwip.Api/Data/Configurations/EvaluationConfiguration.cs` | Konfiguracja EF oceny (T-38): etap i status jako tekst, sześć check constraints (soft delete, data zakończenia sparowana ze statusem, dokładnie jeden autor, znane etapy i statusy, odpowiedzi jako obiekt), złożone klucze obce na `(competition_id, application_id)` i `(competition_id, form_definition_id)`, dwa indeksy częściowe unikalności kart, wszystko `NoAction` |
| `backend/src/Ocwip.Api/Data/Configurations/AttachmentConfiguration.cs` | Konfiguracja EF Core załącznika: limity długości, `format` jako tekst (jak `Requirement`, żeby dołożenie formatu nie przepisywało wierszy), `entity_id` wymagane z osobnym FK bez nawigacji do `entities`, dwa check constraints (dodatni rozmiar, sparowanie soft delete), unikalny indeks na `storage_path`, FK do wniosku bez kaskady (T-32) |
| `backend/src/Ocwip.Api/Data/Migrations/20260828121427_AddApplicationAndAccountModels.cs` | Tabele `users`, `entities`, `applications` i `attachments`, klucz alternatywny `(competition_id, id)` na `form_definitions`, unikalne indeksy na e-mailu, numerze wniosku w konkursie i ścieżce w storage |
| `backend/src/Ocwip.Api/Data/Migrations/20260828121427_AddApplicationAndAccountModels.Designer.cs` | Metadane EF dla `AddApplicationAndAccountModels` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260924095655_AddAttachmentEntityIdAndFormat.cs` | Dokłada `entity_id` (wymagane, FK do `entities`) i `format` (tekst) na `attachments` (T-32) |
| `backend/src/Ocwip.Api/Data/Migrations/20260924095655_AddAttachmentEntityIdAndFormat.Designer.cs` | Metadane EF dla `AddAttachmentEntityIdAndFormat` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260924131449_AddApplicationStatusHistory.cs` | Tworzy tabelę `application_status_history` (T-33): kolumny, check constraint `from_status <> to_status`, dwa FK `NoAction`, dwa indeksy |
| `backend/src/Ocwip.Api/Data/Migrations/20260924131449_AddApplicationStatusHistory.Designer.cs` | Metadane EF dla `AddApplicationStatusHistory` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260925150906_AddApplicationAssignments.cs` | Tworzy tabelę `application_assignments` (T-37): kolumny, check constraint sparowania soft delete, dwa FK `NoAction`, unikalny indeks na parę plus indeks `(reviewer_id, is_active)` |
| `backend/src/Ocwip.Api/Data/Migrations/20260925150906_AddApplicationAssignments.Designer.cs` | Metadane EF dla `AddApplicationAssignments` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260925192721_AddEvaluations.cs` | T-38: `purpose` na `form_definitions` (istniejące wiersze `Application`, bez wartości domyślnej potem), indeks wersji po `(competition_id, purpose, version_number)`, wskazania kart na `competitions`, klucz alternatywny `(competition_id, id)` na `applications`, tabela `evaluations`. `Down()` odmawia, gdy są oceny albo karty |
| `backend/src/Ocwip.Api/Data/Migrations/20260925192721_AddEvaluations.Designer.cs` | Metadane EF dla `AddEvaluations` (generowane) |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/CompetitionConfigurationTests.cs` | Metadane konkursu: nazwa tabeli, klucz, limity długości, status jako tekst |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/CompetitionTimestampConfigurationTests.cs` | Metadane czasu w konkursie: pełna precyzja znaczników audytowych, pełna minuta w oknie, brak konwertera ucinającego |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/CompetitionSchemaConfigurationTests.cs` | Metadane schematu konkursu: wszystkie check constraints, indeks listy publicznej, FK bez kaskady |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/FormDefinitionConfigurationTests.cs` | Metadane modelu definicji formularza: nazwa tabeli, kolumna jsonb, unikalny indeks na `(competition_id, version_number)` |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/FormDefinitionSamples.cs` | Dwie definicje, na których stoją testy kontraktu: jedno pole każdego z piętnastu rodzajów oraz część III wniosku na 2026, czyli tabela kosztów z wartością wiersza, sumą, dotacją z różnicy i limitem procentowym, plus `BudgetWithThreeTables`: tabele A, B i C, koszty razem, dotacja z różnicy i progi B oraz C z ustawień konkursu (T-31) |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/FormSchemaValidatorTests.cs` | Co kontrakt przyjmuje: komplet rodzajów pól, minimum niesione przez każde pole, flaga wydruku (D14), tabela budżetu bez wyjątków, dotacja z różnicy (D11) i limit dający się odwrócić (D12) |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/AnswerSamples.cs` | Odpowiedzi do testów T-30 w kształcie renderera: komplet poprawnych odpowiedzi do `AllFieldKinds` jako `JsonObject`, żeby test psuł dokładnie jedną rzecz, plus skróty do walidacji i odczytu komunikatu po kluczu |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/AnswerValidatorTests.cs` | Dwa poziomy T-30: komplet przechodzi na obu, pusty szkic przechodzi, złożenie wymienia każde brakujące pole, nieznany i podwójny klucz odrzucone, wartość wyliczana odrzucona, klucze komórek jak w rendererze, `maxLength` od razu, `minLength` i zakresy przy złożeniu, niezaznaczone oświadczenie, pole ukryte warunkiem i sekcją niewymagane, liczba wierszy obu rodzajów tabel |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/AnswerShapeTests.cs` | Każdy rodzaj pola odrzuca już w szkicu wartość, której renderer nie mógłby wysłać, i przyjmuje to, co trzyma wyczyszczony albo nieruszony input |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/AnswerLimitsTests.cs` | Limity na próbce budżetu (D11, D12, D13): komunikat z kwotą przekroczenia i granicą, testy graniczne co do złotówki na wyliczonej dotacji, limit procentowy od wyliczonej podstawy, nieustawione ustawienie konkursu, szkic ponad limitem zapisuje się, liczby rzędu 10^20 dają komunikat zamiast wyjątku, pełna precyzja |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/BudgetLimitsTests.cs` | Cztery reguły budżetu z T-31 na trzech tabelach: dotacja pod limitem konkursu (przykład z raportu, dziewięć tysięcy i dziesięć), progi tabel B i C z ustawień konkursu, każda co do złotówki na, pod i nad granicą, komunikat z nazwą tabeli i pozycją przekroczenia, puste ustawienie bez sprawdzania, wartość wiersza nie do podania w żądaniu, pełna precyzja (D13) |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/FormLimitContractTests.cs` | Kontrakt T-31: limit procentowy z procentem i `percentFrom` naraz, z `percentFrom` wskazującym kwotę zamiast progu i limit kwotowy z `percentFrom` odrzucone; kolumna nadal nie sumuje pola spoza tabeli |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/FormFieldRoleContractTests.cs` | Kontrakt roli pola (T-35): trzy role odczytane, odmowa nieznanej, nie pasującej do rodzaju, na ilorazie, na kolumnie i powtórzonej, każda ze ścieżką do pola |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/EvaluationCardSamples.cs` | Dwie małe karty w kształcie kart 2026 (T-38): formalna z kryterium dla wszystkich, dla organizacji i dla grupy z patronem; merytoryczna z punktami, sumą, kryteriami strategicznymi i kwotą rekomendowaną |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/EvaluationCardContractTests.cs` | Kontrakt kart (T-38): przyjęcie obu kart, odmowa karty bez kryterium i bez sumy, `appliesTo`/punktów/ról oceny na wniosku, roli wniosku na karcie, kryterium formalnego na karcie merytorycznej, złej pisowni i pustej listy rodzajów, punktów poza tak lub nie, tak lub nie w iloczynie albo bez punktów |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/EvaluationScoresTests.cs` | Wynik karty (T-38): kryterium niezadane temu wnioskodawcy ani nie oblewa, ani nie punktuje, jedno niespełnione oblewa, brak odpowiedzi zostawia wynik otwarty, pusta kwota to brak rekomendacji, wymagane pole niezadane nie jest brakiem |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/ApplicationRoleValuesTests.cs` | Odczyt wartości z ról: dotacja wyliczona jak w formularzu, formularz bez ról, pusta kwota to nie zero, odpowiedź ukryta warunkiem pominięta |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/PolishNumbersTests.cs` | Te same napisy co `Intl.NumberFormat("pl-PL")` we froncie: grupowanie cyfr od pięciu w górę, dwa miejsca po przecinku w kwocie, procent bez zbędnych zer |
| `backend/tests/Ocwip.Api.Tests/Models/Forms/FormSchemaRefusalTests.cs` | Czego kontrakt nie przyjmie: nieznany rodzaj pola, pole wyliczane bez źródła, obliczenie czytające pole nieistniejące albo nieliczbowe, cykl obliczeń, warunek na pole stojące niżej albo na wartość spoza listy, powtórzony klucz, tabela w tabeli, korzeń inny niż obiekt |
| `backend/tests/Ocwip.Api.Tests/Endpoints/FormDefinitionVersioningTests.cs` | Wersjonowanie formularza po prawdziwym HTTP (T-25): druga publikacja dokłada wersję 2 i zostawia wersję 1 nietkniętą, najnowsza wersja staje się wersją w mocy konkursu, wersja robocza wniosku zostaje na swojej wersji i nadal czyta swój dokument, numer unikalny w konkursie i liczony od nowa w następnym, dwie publikacje naraz nie dają dwóch wierszy z jednym numerem ani 500 |
| `backend/tests/Ocwip.Api.Tests/Endpoints/FormDefinitionRefusalTests.cs` | Czego trasy wersji nie przyjmą (T-25): wnioskodawca i recenzent na każdej z trzech tras, niezalogowany, definicja odrzucona przez bramkę kontraktu (400 ze ścieżką do pola i pusta tabela po odmowie), ciało bez dokumentu, konkurs i wersja nieistniejące, konkurs nieaktywny, oraz dowód NIEOBECNOŚCI trasy podmieniającej opublikowaną wersję |
| `backend/tests/Ocwip.Api.Tests/Endpoints/EvaluationEndpointsTests.cs` | Oceny po prawdziwym HTTP (T-38): operator wypełnia i kończy kartę formalną, ekspert punktuje tylko przypisany wniosek, ekspert nie widzi cudzej karty, wnioskodawca żadnej, operator czyta merytoryczną bez prawa zapisu, cofnięcie przypisania zabiera kartę, wersja karty zamrożona, brak karty i szkic wniosku to 409, karta sprawdzana pod przeznaczenie przy publikacji, karty nie da się wskazać jako formularza wniosku konkursu, dwa check constraints na bazie |
| `backend/tests/Ocwip.Api.Tests/Endpoints/AttachmentEndpointsTests.cs` | Załączniki wniosku po prawdziwym HTTP (T-32): pełny cykl przesłania-pobrania-podmiany z dowodem, że stary wiersz zostaje czytelny ze swoimi własnymi bajtami, format spoza listy odrzucony niezależnie od deklaracji, plik pusty i plik nad limitem odrzucone, drugi plik przekraczający sumę wniosku odrzucony, cudzy podmiot dostaje 403 po zgadniętym identyfikatorze, operator widzi wszystko, upload po zamknięciu naboru (T-21) i po złożeniu wniosku odrzucony. Lista załączników wniosku (T-34): podmieniony wiersz znika, kolejność od najstarszego, cudzy podmiot i nieznany wniosek odmówione |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ApplicationDraftEndpointsTests.cs` | Wersja robocza wniosku po prawdziwym HTTP (T-29): pełny cykl utworzenie-zapis-odczyt-dezaktywacja z sumą kontrolną i czasem zapisu, odmowa bez konkursu, bez opublikowanego formularza i poza oknem naboru (T-21, z dokładnym przypadkiem "minutę po zamknięciu" z checklisty karty), tylko wnioskodawca zakłada wniosek, konto bez podmiotu (B-09) odmówione, cudzy wnioskodawca i recenzent bez dostępu, operator widzi wszystko, złożony wniosek (zasiany ręcznie, bo T-33 jeszcze nie istnieje) już nieedytowalny, korzeń inny niż obiekt odrzucony. Odczyt dokumentu formularza (T-34): wersja przypięta do wniosku, nie najnowsza, cudzy wnioskodawca i nieznany wniosek odmówione |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ApplicationTestHost.cs` | Wspólny punkt startu testów wniosku po HTTP (T-29, T-30): konkurs przyjmujący wnioski z opublikowanym formularzem (domyślnie jedno pole, albo dowolna definicja), publikacja kolejnej wersji, wnioskodawca z podmiotem i zalogowany, skróty do utworzenia, zapisu, odczytu i listowania własnych wniosków (T-34), oraz `SeedReviewerAsync` (T-37): konto recenzenta bez podmiotu, zalogowane, z adresem unikalnym per wywołanie, żeby jeden test mógł zasiać więcej niż jednego recenzenta na tym samym hoście |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ApplicationAnswerValidationTests.cs` | Poziom szkicu T-30 po prawdziwym HTTP: dowolny JSON prosto do API odrzucony z kompletem kluczy pól i niczym nie zapisany, szkic z brakami przechodzi, szkic sprawdzany względem własnej wersji formularza, nie najnowszej, zamknięty nabór odpowiada przed walidacją odpowiedzi |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ApplicationSubmissionEndpointsTests.cs` | Złożenie i PDF potwierdzenia po prawdziwym HTTP (T-33): komplet danych dostaje numer, zamrożenie, wpis historii i jeden e-mail (autozapis żadnego nie wysyła), brak wymaganego pola odrzucony bez nadania czegokolwiek, drugie złożenie odrzucone, zamknięty nabór (T-21), tylko właściciel wnioskodawca składa (operator i recenzent odmówieni na poziomie trasy), nieznany wniosek to 404, dwa równoległe złożenia w jednym konkursie kończą się dwoma różnymi numerami, pobranie potwierdzenia szkicu odrzucone, cudze potwierdzenie 403, operator czyta każde |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ApplicationListEndpointsTests.cs` | Lista wniosków po HTTP (T-35): bez szkiców, wycofanych i cudzego konkursu, 120 wniosków w kolejności numerów, sumy i pula, rodzaj wnioskodawcy tekstem, oferta z aktywnymi załącznikami, 404 dla szkicu, oba eksporty, 403 dla wnioskodawcy i recenzenta na każdej trasie, 401 bez sesji |
| `backend/tests/Ocwip.Api.Tests/Endpoints/ApplicationOverviewEndpointsTests.cs` | "Moje wnioski" po HTTP (T-34): szkic i złożony razem przez dwa konkursy, kilka ofert tego samego podmiotu w jednym konkursie jako osobne wiersze, szkic dezaktywowany znika z listy, cudzy wnioskodawca nigdy nie widzi, konto bez podmiotu odmówione, operator i recenzent bez dostępu, 401 bez sesji |
| `backend/tests/Ocwip.Api.Tests/Data/TestModel.cs` | Model EF budowany tak jak w aplikacji, przez `IDesignTimeModel`, bez łączenia z bazą. Model runtime gubi check constraints i komentarze |
| `backend/tests/Ocwip.Api.Tests/Data/PostgresDatabaseFixture.cs` | Jednorazowa baza na klasę testową: CREATE DATABASE, migracje, DROP. Milczy bez connection stringa, bo xUnit tworzy fixture nawet dla pominiętych testów |
| `backend/tests/Ocwip.Api.Tests/Data/ThrowawayDatabase.cs` | Baza zakładana na jeden test i kasowana po nim. Odróżnia się od `PostgresDatabaseFixture` tym, że **nie migruje**: test o migracji musi zobaczyć bazę przed jej wykonaniem |
| `backend/tests/Ocwip.Api.Tests/Data/PostgresCollection.cs` | Kolekcja `postgres`: wszystkie testy dotykające bazy w jednej kolekcji, jedna baza jako `ICollectionFixture`. Równoległe `CREATE DATABASE` kopiuje `template1` i wywala się na 55006 |
| `backend/tests/Ocwip.Api.Tests/Data/CompetitionDatabaseTests.cs` | Reszta niezmienników konkursu na prawdziwym PostgreSQL: kwota, status jako tekst, szerokości kolumn, insert omijający EF |
| `backend/tests/Ocwip.Api.Tests/Data/CompetitionWindowDatabaseTests.cs` | Okno konkursu na prawdziwym PostgreSQL: kolejność dat, pełna minuta na obu końcach, offset `+02:00`, regresja na operandzie zapytania |
| `backend/tests/Ocwip.Api.Tests/Data/CompetitionLifecycleDatabaseTests.cs` | Soft delete i znaczniki audytowe na prawdziwym PostgreSQL: sparowanie `is_active` z `deactivated_at`, ruch `updated_at` |
| `backend/tests/Ocwip.Api.Tests/Data/CompetitionLifecycleSchemaDatabaseTests.cs` | Kolumny z T-20 na prawdziwym PostgreSQL: sparowanie `end_date` z `is_continuous_intake` w obie strony, unikalny numer wśród aktywnych i oddawany przy dezaktywacji, złożony FK odrzucający cudzy formularz |
| `backend/tests/Ocwip.Api.Tests/Data/CompetitionParametersSchemaDatabaseTests.cs` | Parametry z `T-20a` na prawdziwym PostgreSQL: sparowanie pól papieru w trzy strony, minimalna dotacja w granicach maksymalnej, procent poza zakresem, próg przychodu równy zero jako ustawienie, ramy projektu (także jednodniowe), limity uploadu, załącznik bez formatu, formaty zapisane tekstem, unikalność kategorii i osoby kontaktowej |
| `backend/tests/Ocwip.Api.Tests/Models/CompetitionStatusTransitionsTests.cs` | Tabela przejść jako zbiór: brak powtórzonej pary, `AllowsOperator` odrzuca parę terminową, brak cofania i przeskoków, `Archived` bez wyjścia, każdy stan osiągalny, tylko okno naboru napędzane zegarem |
| `backend/tests/Ocwip.Api.Tests/Models/CompetitionLifecycleTests.cs` | Stan efektywny: szkic nie publikuje się sam, minuta przed i minuta zamknięcia (D7), nabór ciągły nigdy się nie zamyka, dwa kroki terminowe w jednej chwili, tylko szkic bez publicznego adresu |
| `backend/tests/Ocwip.Api.Tests/Models/CompetitionIntakeTests.cs` | Reguła odcięcia terminu: minuta przed, minuta zamknięcia i minuta po (D7), nabór ciągły bez momentu zamknięcia, szkic i konkurs nieaktywny jako brak naboru, stany po naborze, przejście czasu letniego na zimowy, tylko jeden stan przyjmujący wnioski |
| `backend/tests/Ocwip.Api.Tests/Models/ApplicationChecksumTests.cs` | Suma kontrolna (D15) bez bazy: kształt trzech grup po cztery znaki, stabilna dla tego samego wejścia, niezależna od kolejności kluczy i białych znaków (to, co PostgreSQL zrobi z `jsonb`), zmienia się z treścią i z ostatnim zapisem, dwa wnioski o identycznych odpowiedziach nie dzielą sumy, kolejność w tablicy się liczy |
| `backend/tests/Ocwip.Api.Tests/Data/FormDefinitionConstraintDatabaseTests.cs` | Czego schemat nie przyjmie w definicji formularza: `version_number` niedodatni, korzeń JSON-a jako skalar |
| `backend/tests/Ocwip.Api.Tests/Data/TestCompetition.cs` | Konkurs spełniający wszystkie check constraints, żeby test mówił tylko o swoim jednym polu |
| `backend/tests/Ocwip.Api.Tests/Data/PostgresAssert.cs` | Wyłuskuje `PostgresException` z `DbUpdateException` plus stałe SQLSTATE, w tym `P0001` z `RAISE EXCEPTION`, żeby test twierdził o nazwie constraintu, nie o komunikacie |
| `backend/tests/Ocwip.Api.Tests/Data/FormDefinitionDatabaseTests.cs` | Niezmienniki definicji formularza na prawdziwym PostgreSQL: unikalna wersja w konkursie, FK bez kaskady, round trip jsonb |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/ApplicationConfigurationTests.cs` | Metadane modelu wniosku: nazwa tabeli, kolumna jsonb, status jako tekst, opcjonalność numeru i daty złożenia, znaczniki czasu z konwerterem UTC |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/ApplicationStatusHistoryConfigurationTests.cs` | Metadane historii statusów (T-33): nazwa tabeli, klucz główny, `FromStatus`/`ToStatus` jako tekst, check constraint `from_status <> to_status`, indeks na `(application_id, changed_at)`, oba FK `NoAction` |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/ApplicationSchemaConfigurationTests.cs` | Metadane schematu wniosku: check constraints, indeksy, brak kaskady, złożony FK na `(competition_id, id)` definicji formularza oraz dowód NIEOBECNOŚCI unikalności na `(entity_id, competition_id)` |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/AttachmentConfigurationTests.cs` | Metadane modelu załącznika: limity długości, `long` na rozmiar, `format` jako tekst ograniczony, `entity_id` wymagany z FK bez kaskady, dwa check constraints, unikalny `storage_path`, komentarze wskazujące T-32 |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/AccountConfigurationTests.cs` | Metadane konta i podmiotu: nazwy tabel, unikalny indeks na `normalized_email`, sparowanie sentinela `lockout_enabled` z jego wartością domyślną, dowody NIEOBECNOŚCI (brak unikalności na samym e-mailu, brak zmapowania trzech nieużywanych kolumn Identity), enumy jako tekst, nullowalność pól wrażliwych, regresja na `DateTimeOffset` zamiast `DateTime`, relacja 1:1 bez kaskady, brak kaskady na **każdej** relacji w modelu, oba stampy Identity wymagane z wartością domyślną |
| `backend/tests/Ocwip.Api.Tests/Data/ApplicationDatabaseTests.cs` | Niezmienniki wniosku na prawdziwym PostgreSQL: kilka ofert jednego podmiotu w jednym konkursie, brak kaskady na konkurs, podmiot i wersję formularza, round trip jsonb |
| `backend/tests/Ocwip.Api.Tests/Data/ApplicationConstraintDatabaseTests.cs` | Czego schemat nie przyjmie we wniosku: konkurs niebędący właścicielem wersji formularza, połowiczne złożenie, numer na wersji roboczej, duplikat numeru w konkursie, odpowiedzi jako skalar |
| `backend/tests/Ocwip.Api.Tests/Data/ApplicationStatusHistoryDatabaseTests.cs` | Czego schemat nie przyjmie w historii statusów (T-33): przejście, które nie zmienia statusu. Plus dowód, że prawdziwe przejście dopisuje się i zostaje przy swoim wniosku |
| `backend/tests/Ocwip.Api.Tests/Data/AttachmentDatabaseTests.cs` | Niezmienniki załącznika na prawdziwym PostgreSQL: niedodatni rozmiar, duplikat ścieżki w storage, brak kaskady na wniosek i na podmiot |
| `backend/tests/Ocwip.Api.Tests/Data/NormalizedAddressTests.cs` | Przypina `upper(normalize(<adres>, NFC))` z PostgreSQL do **normalizatora Identity**, nie do przepisanego `ToUpperInvariant()`. Adresy z polskimi znakami, bo lista czysto ASCII nie ma jak paść, adresy ze znakami łączącymi (NFD), osobny fakt dowodzący, że samo `upper()` rozjeżdża się z .NET, plus osobny na jedyny rozjazd, którego `normalize()` nie zamyka: niemieckie ostre s |
| `backend/tests/Ocwip.Api.Tests/Data/AccountDatabaseTests.cs` | Niezmienniki konta i podmiotu na prawdziwym PostgreSQL: duplikat e-maila, duplikat różniący się tylko wielkością liter, insert obok EF lądujący na defaultach Identity razem z oboma stampami, dwa takie inserty dostające dwa **różne** stampy, konto zapisane z wyłączoną blokadą naprawdę ją ma wyłączoną (sentinel EF przy defaulcie `true`), dwa konta na jeden podmiot, konto bez podmiotu, podmiot bez NIP-u, regresja na offsecie `+02:00` |
| `backend/tests/Ocwip.Api.Tests/Data/TestEntity.cs` | Podmiot spełniający wszystkie constrainty, żeby test mówił tylko o swoim jednym polu |
| `backend/tests/Ocwip.Api.Tests/Data/TestUser.cs` | Konto spełniające wszystkie constrainty, razem z kolumnami znormalizowanymi przez `EmailNormalizer`, które w produkcji wypełnia `UserManager`, i z jawnie ustawioną blokadą. Hash jest jawnym placeholderem, nic tu nie ma wyglądać jak poświadczenie |
| `backend/tests/Ocwip.Api.Tests/Data/TestApplicationChain.cs` | `ApplicationChain` plus `TestApplicationChain.SeedAsync`: zasiewa konkurs, wersję definicji formularza i podmiot, bez których wniosek nie może istnieć |
| `backend/tests/Ocwip.Api.Tests/Data/TestApplication.cs` | `Draft` i `Submitted`: dwie fabryki, bo schemat paruje status z datą złożenia i numerem, a jedna fabryka pozwoliłaby zbudować kształt odrzucany przez bazę |
| `backend/tests/Ocwip.Api.Tests/Data/TestAttachment.cs` | Załącznik spełniający wszystkie constrainty, z losową nieprzewidywalną ścieżką w storage. Przyjmuje `entityId` osobno od `applicationId`, żeby test mógł twierdzić o obu kluczach niezależnie |
| `backend/tests/Ocwip.Api.Tests/Services/AttachmentFormatDetectorTests.cs` | Wykrywanie formatu z sygnatury bajtów (T-32): każdy z ośmiu formatów, sygnatura ZIP i OLE rozstrzygana rozszerzeniem, niezgodność bajtów z rozszerzeniem odrzucona, śmieci odrzucone, dowód że deklarowany typ treści nigdzie nie wchodzi do decyzji |
| `backend/tests/Ocwip.Api.Tests/Services/ApplicationListExportTests.cs` | Eksporty listy: CSV z BOM, średnikiem i kwotami z przecinkiem, rozbrajanie formuł bez psucia liczb ujemnych, pusta pula jako pusta komórka, 120 wierszy na trzech stronach PDF z nagłówkiem na każdej, potwierdzenie nadal jednostronicowe |
| `backend/tests/Ocwip.Api.Tests/Services/AttachmentStorageServiceTests.cs` | Dysk lokalny (T-32): zapis i odczyt tego samego pliku, dwa uploady nigdy nie dzielą ścieżki, ścieżka bez śladu nazwy, podmiana nie kasuje poprzedniego pliku |

| `backend/src/Ocwip.Api/Admin/AdminCommandLine.cs` | `GrantRoleRequest` plus parsowanie linii poleceń komendy `grant-role`: `IsAdminInvocation` (pierwszy argument nie zaczynający się od `-` ani `/`), `--email` i `--role`, tekst `Usage`. Bez konsoli i bez bazy. Rola dopasowywana po nazwie, nie przez `Enum.TryParse`, który przyjąłby `1`. Adres nie jest obcinany z białych znaków |
| `backend/src/Ocwip.Api/Admin/GrantRoleCommand.cs` | `GrantRoleOutcome` plus `ExecuteAsync`: jedyne miejsce w kodzie zapisujące kolumnę `role`. Dopasowuje konto po `normalized_email` przez `EmailNormalizer`, czyli przez ten sam normalizator co Identity, więc ani wielkość liter, ani sposób zapisu akcentu w podanym adresie nie ma znaczenia. Odmawia kontu nieistniejącemu i dezaktywowanemu, nie zapisuje przy roli już posiadanej |
| `backend/src/Ocwip.Api/Admin/AdminCommandRunner.cs` | Sklejenie komendy ze światem: konfiguracja przez `AppDbContextFactory`, komunikaty na `TextWriter`, kody wyjścia `Success` i `Failure`. Awaria bazy kończy się kodem 1 i jednym zdaniem, nie nieobsłużonym wyjątkiem i kodem 134; `DbUpdateConcurrencyException` ma osobny `catch`, bo `ConcurrencyStamp` jest tokenem współbieżności, a `DbUpdateException` nie dziedziczy po żadnym typie z filtra ogólnego |
| `backend/src/Ocwip.Api/Data/Migrations/20260902064108_AddUserRoleDefaultAndConstraint.cs` | Wartość domyślna `'Applicant'` na `users.role` i check constraint `ck_users_role_is_known`. `Down()` odwraca oba |
| `backend/src/Ocwip.Api/Data/Migrations/20260902064108_AddUserRoleDefaultAndConstraint.Designer.cs` | Metadane EF dla `AddUserRoleDefaultAndConstraint` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260904095732_AddIdentityToUsers.cs` | `ALTER TABLE users` do kształtu Identity, nie nowa tabela: kolumny Identity z defaultami, `is_verified` i unikalny indeks na e-mailu usunięte, unikalny `ix_users_normalized_email`, trzy tabele `user_*`. **Ręcznie poprawiona** w czterech miejscach: zaczyna od bloku `DO` zatrzymującego migrację z listą adresów kolidujących wielkością liter albo zapisem akcentu, `normalized_email` wchodzi jako nullable, zostaje wypełnione `upper(normalize(email, NFC))` i dopiero potem staje się `NOT NULL` (scaffold dawał wszystkim wierszom `''` i indeks padał na drugim), oba stampy przechodzą tą samą drogą do `NOT NULL` z `gen_random_uuid()::text`, a `is_verified` jest kasowane **po** przepisaniu wartości do `email_confirmed`. Klucze obce tabel `user_*` mają `NoAction`, nie `Cascade`. `Down()` przepisuje w drugą stronę, ale świadomie nie jest w pełni odwracalne |
| `backend/src/Ocwip.Api/Data/Migrations/20260904095732_AddIdentityToUsers.Designer.cs` | Metadane EF dla `AddIdentityToUsers` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260907110649_SyncUserIsActiveSnapshot.cs` | Domyka `IsActive` w snapshocie po jego dopisaniu do `User.cs`/`UserConfiguration.cs`. Efekt uboczny: `dotnet ef migrations add` nie widział defaultu `AccessFailedCount` we fluent configuration i sam go zdjął (`AlterColumn` bez `defaultValue`) - niezamierzone, odwrócone kolejną migracją |
| `backend/src/Ocwip.Api/Data/Migrations/20260907110649_SyncUserIsActiveSnapshot.Designer.cs` | Metadane EF dla `SyncUserIsActiveSnapshot` (generowane) |
| `backend/src/Ocwip.Api/Data/Migrations/20260907111921_RestoreAccessFailedCountDefault.cs` | Przywraca `DEFAULT 0` na `access_failed_count`, zdjęty przypadkiem przez `SyncUserIsActiveSnapshot`. Default jest teraz też zadeklarowany w `UserConfiguration.cs`, więc kolejny `dotnet ef migrations add` już go nie zdejmie |
| `backend/src/Ocwip.Api/Data/Migrations/20260907111921_RestoreAccessFailedCountDefault.Designer.cs` | Metadane EF dla `RestoreAccessFailedCountDefault` (generowane) |
| `backend/tests/Ocwip.Api.Tests/Data/Configurations/UserRoleConfigurationTests.cs` | Metadane kolumny roli: trzy nazwy zgodne z regułami biznesowymi, `Applicant` jako wartość domyślna w CLR i w schemacie, zgodność listy w check constraincie z enumem w obie strony oraz dowód NIEOBECNOŚCI unikalności na roli |
| `backend/tests/Ocwip.Api.Tests/Data/UserRoleDatabaseTests.cs` | Rola na prawdziwym PostgreSQL: insert bez kolumny daje `Applicant`, `role = 'operator'` małą literą wywala 23514, dwa konta trzymają rolę operatora naraz |
| `backend/tests/Ocwip.Api.Tests/Admin/AdminCommandLineTests.cs` | Parsowanie komendy bez bazy i bez konsoli: argumenty hosta nie są komendą, literówka w czasowniku nadal nią jest, rola bez względu na wielkość liter, adres z białymi znakami nietknięty, odrzucenie roli numerycznej, brakującej wartości, nieznanej opcji i opcji podanej dwa razy |
| `backend/tests/Ocwip.Api.Tests/Admin/GrantRoleCommandTests.cs` | Nadawanie roli na prawdziwym PostgreSQL: awans, drugi operator, adres bez trafienia, adres inną wielkością liter oraz adres z akcentem zapisanym rozkładowo trafiające w to samo konto, konto dezaktywowane, powtórzone nadanie nie rusza `updated_at`, odebranie roli |
| `backend/tests/Ocwip.Api.Tests/Admin/AdminCommandRunnerTests.cs` | Kody wyjścia i komunikaty: zła linia poleceń drukuje powód plus `Usage`, brak connection stringa jest raportowany, a nie zgadywany, nieosiągalna baza daje kod 1 bez stosu wywołań, literówka w czasowniku nie startuje serwera, nieznana komenda nie dotyka bazy, a zapis trafiający zero wierszy (konflikt współbieżności) daje kod 1 z komunikatem o powtórzeniu komendy, nie stos wywołań |
| `backend/tests/Ocwip.Api.Tests/Endpoints/RegistrationTests.cs`| Testy dla rejestracji |

## Czego tu jeszcze nie ma

`Services/`, `Contracts/` i `Endpoints/` mają już realną zawartość (rejestracja na `RegistrationResult`, weryfikacja e-mail, sonda zdrowia), ale logowanie i sesja nadal nie istnieją: T-12.1/T-12.2 kończą się na koncie utworzonym i potwierdzonym, nie na kimś zalogowanym. Sesja w ciasteczku HttpOnly to decyzja w [`architektura.md`](architektura.md), jeszcze bez endpointu. Prawdziwy dostawca e-mail (SMTP czy podobny) też nie istnieje: `EmailSenderService.cs` tylko loguje treść, to świadoma decyzja pierwszej wersji. `Admin/` istnieje i mieści wyłącznie komendy uruchamiane z powłoki, nigdy nic wystawionego po HTTP. Encje domenowe żyją w `Models/`, nie w `Domain/`. Każde ma kartę na Trello. Katalogi zakładamy razem z pierwszym prawdziwym plikiem.
